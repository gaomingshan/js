你是一名资深 MongoDB 架构师与技术教育者，在当前目录下，请生成 MongoDB 系统化学习内容及面试题，要求如下：

【学习对象】
- 有后端开发经验，但未系统学习 MongoDB
- 已使用过 MongoDB，但对存储引擎、索引机制、复制集、分片等理解碎片化
- 希望建立文档数据库设计思想、分布式架构、查询优化及企业级实践能力
- 需要从原生 API 到 Spring Boot/Spring Cloud 集成的完整技术路径

【生成顺序与文件结构】
1. 生成大纲文件 content-outline.md，每个小节带有 Markdown 跳转链接到对应 content 文件
2. 生成教学内容，存放于 content/ 目录
3. 生成面试题汇总，存放于 quiz/quiz.md

【文件命名规则】
- 大纲文件：content-outline.md
- 教学内容：content/content-1.md、content/content-2.md … 按大纲顺序递增
- 面试题汇总：quiz/quiz.md
- 链接格式：[小节标题](./content/content-x.md)，x 按大纲顺序递增
- 统一使用 Markdown，中文，技术严谨且教学友好

【教学内容要求】
- 核心思想：文档模型设计 + 存储引擎机制 + 分布式架构（复制集/分片）+ 查询执行流程
- 每小节包含：
  - 核心概念与原理
  - 架构设计动机
  - 易错点/陷阱/边界
  - 企业级实践示例
  - 性能优化建议
- 精简但完整，避免简单罗列配置项
- 难度分级：基础 → 进阶 → 高级
- 必须体现从单机到分布式、从原生到 Spring 的渐进式学习路径
- 教学内容和示例要覆盖真实企业场景（用户中心、订单系统、日志分析、监控审计、内容平台等）

【必须覆盖核心主题（可在此基础上优化章节结构）】

  **第一部分：基础架构与核心概念**
  - MongoDB 核心概念（Database、Collection、Document、BSON）
  - 文档模型 vs 关系模型
  - MongoDB 架构组成（mongod、mongos、config server）
  - 存储引擎概览（WiredTiger）
  - 数据模型设计（嵌入式文档、引用关系、范式与反范式）

  **第二部分：环境搭建（单体 → 容器 → 云原生）**
  - 单机安装与基础配置（Windows/Linux/macOS）
  - Docker / Docker Compose 部署
  - Kubernetes（StatefulSet + PV + PDB）部署思路
  - MongoDB Atlas 云服务入门
  - 安全初始化（用户、角色、认证、TLS）
  - 环境差异与版本策略（社区版/企业版/云服务）

  **第三部分：数据写入与存储机制**
  - 写入路径（客户端 → Primary → Journal → Checkpoint）
  - WiredTiger 页、压缩、缓存机制
  - Journal 与崩溃恢复
  - Write Concern / Read Concern / Read Preference
  - 批量写入与幂等设计
  - TTL、Capped Collection、Time Series Collection 使用场景

  **第四部分：索引**
  - 单字段、复合、唯一、稀疏、部分索引
  - 多键索引、文本索引、地理空间索引、哈希索引
  - 覆盖索引与索引前缀规则
  - 索引构建策略（前台/后台、滚动构建）
  - 索引生命周期管理与冗余索引治理

  **第五部分：查询**
  - 查询语法与操作符（匹配、范围、数组、嵌套）
  - 聚合管道（$match、$group、$project、$lookup、$facet）
  - 查询执行计划（explain）
  - 分页策略（skip/limit vs 游标分页）
  - 查询性能优化（hint、投影、排序与索引协同）

  **第六部分：分布式（复制集与分片）**
  - 复制集架构与选主机制（Raft/PV）
  - Oplog 同步与复制延迟分析
  - 分片架构（mongos/config server/shard）
  - 分片键设计（范围/哈希/复合）
  - Chunk 迁移与均衡器
  - 跨区域部署与容灾（多可用区、多地域）

  **第七部分：调优与监控**
  - 慢查询分析与系统瓶颈定位
  - 内存/磁盘/连接池调优
  - 关键指标（opcounters、wiredTiger、locks、replication lag）
  - Profiling 与日志诊断
  - 备份恢复（mongodump、快照、PITR）
  - 容量规划与扩容策略

  **第八部分：原生 API 接入**
  - Mongo Shell / mongosh 常用操作
  - 原生 CRUD API（insert/find/update/delete）
  - Bulk Write、事务 API、Change Streams
  - 聚合 API、索引 API、管理 API
  - 错误处理、重试、幂等与超时控制

  **第九部分：Spring 集成（Spring Boot）**
  - Spring Data MongoDB 架构
  - 配置与连接管理（连接串、认证、连接池）
  - Repository 模式与 MongoTemplate 选择
  - 注解映射（@Document、@Field、@Indexed）
  - 聚合查询封装与分页
  - 事务支持与限制

  **第十部分：Spring Cloud 集成**
  - 微服务中 MongoDB 的边界与职责
  - 配置中心管理 Mongo 参数
  - 服务发现与多集群访问
  - 分布式事务替代方案（事件驱动/Saga）
  - 统一日志与审计链路（ELK/OTel）

  **第十一部分：企业最佳实践**
  - 高并发订单与库存场景建模
  - 用户画像与内容推荐场景
  - 多租户隔离（库级/集合级/字段级）
  - 安全合规（最小权限、审计、加密）
  - 数据生命周期（冷热分层、归档、清理）
  - 生产变更规范（灰度、回滚、演练）

  **第十二部分：高级特性**
  - 多文档事务与一致性边界
  - Change Streams 事件驱动架构
  - Time Series 与 IoT 场景
  - Atlas Search / 向量检索（Vector Search）
  - Field Level Encryption（FLE）
  - Schema Validation 与治理

【面试题要求】
- 题型：单选 / 多选 / 场景题 / 架构设计题结合，约 100-150 题
- 难度递进：基础概念 → 原理机制 → 性能调优 → 架构设计
- 题目覆盖大纲内容，侧重实际应用与问题解决
- 每题提供答案与解析，解析分层：
  - 答案与结论
  - 原理分析
  - 最佳实践建议
  - 易错点与陷阱
- 面试题仅生成一个汇总文件 quiz/quiz.md
- 面试题解析超过 15 行 Markdown 必须拆分，保证可读性
- 必须包含架构设计题（如：设计一个支撑亿级用户与高峰写入的 MongoDB 平台）

【大纲设计原则】
- 学习路径清晰：基础概念 → 单机部署 → 分布式架构 → 查询优化 → 企业实践
- 从原生 API 到 Spring 生态的渐进式集成路径
- 逻辑递进，章节之间有依赖关系（如：先理解文档模型，再学索引与查询）
- 每章节标题明确、简洁，并说明本章解决的核心问题
- 避免简单罗列配置项或 API 文档
- 保证核心概念（存储引擎、索引、查询执行、复制、分片、一致性）覆盖完整
- 体现从理论到实践的完整闭环

【质量红线】
- 禁止空洞描述、简单堆砌配置项、翻译官方文档
- 小节概念不清晰时应精简而非膨胀
- 所有示例必须来自真实企业场景
- 原理解释必须与 MongoDB 存储引擎与分布式一致性机制一致
- 不允许混淆 MongoDB、关系数据库、搜索引擎（如 ES）的边界
- 架构设计必须考虑高可用、高性能、可扩展性
- 必须明确说明版本差异（如：4.4、5.0、6.x、7.x、8.x 的重大变化）

【配置与最佳实践要求】
- mongod.conf 核心配置详解
- 存储引擎配置（cache、compression、journal）
- 复制集配置（members、priority、votes、arbiter）
- 分片配置（chunkSize、balancer、zone sharding）
- 安全配置（SCRAM、x.509、TLS、审计）
- 每个配置项必须说明使用场景与调优思路

【原生控制台接口要求】
- 提供完整的 mongosh 与驱动 API 示例
- 覆盖 CRUD、聚合、索引、事务、管理等常用场景
- 展示请求与响应结构
- 说明关键参数与返回字段含义
- 提供错误排查与调试技巧

【Spring 集成要求】
- MongoDB Java Driver 与 Spring Data 的对比
- 同步驱动 vs Reactive 驱动
- Spring Boot Starter 配置与自动装配原理
- Repository 模式与 MongoTemplate 的选择
- 连接池管理与性能优化
- 异常处理、重试与幂等策略
- 与 Spring Cloud 组件整合（Config、Gateway、Nacos/Eureka）

【最终目标】
- 一条 Prompt 生成：
  1. content-outline.md（大纲）
  2. content/ 目录下的章节内容（教学 + 示例 + 配置）
  3. quiz/quiz.md（面试题）
- 确保学习者：
  - 形成完整、正确的 MongoDB 架构心智模型
  - 掌握从单机到复制集与分片、从开发到运维的完整技术栈
  - 能够独立设计和实施企业级数据平台方案
  - 具备性能调优、故障诊断、架构设计能力
  - 熟练使用原生 API 与 Spring 生态集成
  - 能顺利应对 MongoDB 相关面试与实际项目挑战

【特别强调】
- 必须深入讲解复制集与分片原理，不能只停留在使用层面
- 必须包含完整的从 0 到 1 的环境搭建实践（单机、容器、K8s、云）
- 必须提供真实企业级架构案例（如：亿级用户、TB~PB 数据）
- 必须覆盖常见问题与解决方案（如：主从延迟、热点分片、慢查询、OOM、事务冲突）
- 必须体现版本演进与升级路径（从 4.4 到 8.x 的关键变化）
