# 第 16 章：FFC 弹性格式化上下文 - 面试题

**难度分布：** 🟢 简单 x3 | 🟡 中等 x4 | 🔴 困难 x3

---

## 第 1 题 🟢

**类型：** 单选题  
**标签：** FFC 定义

### 题目

FFC（Flex Formatting Context）是由什么创建的？

**选项：**
- A. `display: block`
- B. `display: flex` 或 `display: inline-flex`
- C. `float: left`
- D. `position: relative`

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**FFC - 弹性格式化上下文**

```css
.container {
  display: flex;         /* 块级 flex 容器，创建 FFC */
}

.inline-container {
  display: inline-flex;  /* 行内 flex 容器，创建 FFC */
}
```

**FFC 的特点：**
- Flex 容器的子元素按弹性布局排列
- 不再遵循正常流规则
- margin 不会合并
- float/clear/vertical-align 无效

</details>

---

## 第 2 题 🟢

**类型：** 单选题  
**标签：** Flex 容器

### 题目

在 FFC 中，flex 容器的子元素称为？

**选项：**
- A. flex item
- B. flex child
- C. flex element
- D. flex box

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A

### 📖 解析

**Flex 术语**

```css
.container {
  display: flex;  /* flex 容器 */
}
```

```html
<div class="container">
  <div>Flex Item 1</div>  <!-- flex 项目 -->
  <div>Flex Item 2</div>  <!-- flex 项目 -->
</div>
```

**核心概念：**
- **Flex容器**（Flex Container）：设置 `display: flex` 的元素
- **Flex项目**（Flex Item）：flex 容器的直接子元素
- **主轴**（Main Axis）：flex 项目排列的方向
- **交叉轴**（Cross Axis）：垂直于主轴的方向

</details>

---

## 第 3 题 🟢

**类型：** 判断题  
**标签：** Margin 合并

### 题目

在 FFC 中，flex 项目的 margin 会发生合并。

**选项：**
- A. ✅ 正确
- B. ❌ 错误

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B（错误）

### 📖 解析

**FFC 中 margin 不合并**

```css
.container {
  display: flex;
}

.item {
  margin: 20px;
}
```

```html
<div class="container">
  <div class="item">Item 1</div>
  <div class="item">Item 2</div>
</div>
<!-- 两个 item 之间的间距 = 20px + 20px = 40px -->
```

**对比正常流：**
```css
/* 正常流中 margin 会合并 */
.block {
  margin: 20px;
}

<div class="block">Block 1</div>
<div class="block">Block 2</div>
<!-- 间距 = max(20px, 20px) = 20px -->
```

**FFC 的特殊性：**
- margin 不合并
- float 无效
- clear 无效
- vertical-align 无效

</details>

---

## 第 4 题 🟡

**类型：** 多选题  
**标签：** Flex Item 特性

### 题目

成为 flex item 后，元素会发生哪些变化？

**选项：**
- A. float 属性失效
- B. vertical-align 属性失效
- C. margin 不会合并
- D. 可以使用 flex 属性

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, C, D

### 📖 解析

**Flex Item 特性（全部正确）**

**✅ A. float 失效**
```css
.container {
  display: flex;
}

.item {
  float: left;  /* 无效 */
}
```

**✅ B. vertical-align 失效**
```css
.item {
  vertical-align: middle;  /* 无效 */
}
/* 使用 align-items/align-self 代替 */
```

**✅ C. margin 不合并**
```css
.item {
  margin: 10px;
}
/* margin 完全保留，不合并 */
```

**✅ D. 可用 flex 属性**
```css
.item {
  flex: 1;  /* flex-grow: 1 */
  flex-shrink: 0;
  flex-basis: auto;
}
```

**完整对比：**

| 属性/特性 | 正常流 | FFC |
|----------|--------|-----|
| float | ✅ 有效 | ❌ 无效 |
| vertical-align | ✅ 有效 | ❌ 无效 |
| margin合并 | ✅ 合并 | ❌ 不合并 |
| flex属性 | ❌ 无效 | ✅ 有效 |

</details>

---

## 第 5 题 🟡

**类型：** 代码题  
**标签：** 主轴方向

### 题目

以下代码中，flex 项目的排列方向是？

```css
.container {
  display: flex;
  flex-direction: column;
}
```

**选项：**
- A. 从左到右
- B. 从右到左
- C. 从上到下
- D. 从下到上

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**flex-direction 属性**

```css
/* 水平方向 */
flex-direction: row;          /* 从左到右（默认）*/
flex-direction: row-reverse;  /* 从右到左 */

/* 垂直方向 */
flex-direction: column;         /* 从上到下 ✅ */
flex-direction: column-reverse; /* 从下到上 */
```

**主轴和交叉轴：**

**row（默认）**
```
主轴 →
┌───┬───┬───┐
│ 1 │ 2 │ 3 │
└───┴───┴───┘
交叉轴 ↓
```

**column**
```
主轴
↓
┌───┐ ← 交叉轴 →
│ 1 │
├───┤
│ 2 │
├───┤
│ 3 │
└───┘
```

**影响属性：**
- `justify-content` - 主轴对齐
- `align-items` - 交叉轴对齐
- `flex-grow/shrink` - 主轴方向伸缩

</details>

---

## 第 6 题 🟡

**类型：** 代码题  
**标签：** Flex 换行

### 题目

`flex-wrap: wrap` 的作用是？

**选项：**
- A. 禁止换行
- B. 允许换行
- C. 反向换行
- D. 自动换行

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**flex-wrap 属性**

```css
.container {
  display: flex;
  flex-wrap: wrap;  /* 允许换行 */
}
```

**所有值：**

**nowrap（默认）**
```css
flex-wrap: nowrap;
/* 不换行，压缩 flex 项目 */
```

**wrap**
```css
flex-wrap: wrap;
/* 换行，第一行在上方 */
```

**wrap-reverse**
```css
flex-wrap: wrap-reverse;
/* 换行，第一行在下方 */
```

**示例：**

**nowrap**
```
┌──────────────────┐
│ [1][2][3][4][5] │ ← 压缩显示
└──────────────────┘
```

**wrap**
```
┌──────────────────┐
│ [1][2][3]        │ ← 第一行
│ [4][5]           │ ← 第二行
└──────────────────┘
```

**wrap-reverse**
```
┌──────────────────┐
│ [4][5]           │ ← 第二行（在上）
│ [1][2][3]        │ ← 第一行（在下）
└──────────────────┘
```

</details>

---

## 第 7 题 🟡

**类型：** 单选题  
**标签：** 对齐方式

### 题目

在 FFC 中，控制主轴对齐的属性是？

**选项：**
- A. `align-items`
- B. `justify-content`
- C. `align-content`
- D. `place-items`

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**Flexbox 对齐属性**

**主轴对齐：justify-content**
```css
.container {
  display: flex;
  justify-content: center;  /* 主轴居中 */
}
```

**交叉轴对齐：align-items**
```css
.container {
  align-items: center;  /* 交叉轴居中 */
}
```

**多行对齐：align-content**
```css
.container {
  flex-wrap: wrap;
  align-content: space-between;  /* 多行分布 */
}
```

**对比表：**

| 属性 | 作用轴 | 单行/多行 |
|------|-------|----------|
| justify-content | 主轴 | 单行 |
| align-items | 交叉轴 | 单行 |
| align-content | 交叉轴 | 多行 |
| align-self | 交叉轴 | 单个项目 |

**justify-content 值：**
```css
justify-content: flex-start;    /* 起始对齐 */
justify-content: flex-end;      /* 末尾对齐 */
justify-content: center;        /* 居中 */
justify-content: space-between; /* 两端对齐 */
justify-content: space-around;  /* 分散对齐 */
justify-content: space-evenly;  /* 均匀分布 */
```

</details>

---

## 第 8 题 🔴

**类型：** 代码题  
**标签：** Flex 计算

### 题目

以下代码中，3个 flex item 的宽度分别是？

```css
.container {
  display: flex;
  width: 600px;
}

.item1 { flex: 1; }  /* flex-basis: 0 */
.item2 { flex: 2; }
.item3 { flex: 1; }
```

**选项：**
- A. 200px, 200px, 200px
- B. 150px, 300px, 150px
- C. 100px, 400px, 100px
- D. 由内容决定

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**Flex 计算规则**

**flex 简写：**
```css
flex: 1;  /* = flex: 1 1 0% */
/* flex-grow: 1 */
/* flex-shrink: 1 */
/* flex-basis: 0% */
```

**计算过程：**

**1. 总宽度**
```
容器：600px
```

**2. flex-grow 总和**
```
1 + 2 + 1 = 4
```

**3. 分配宽度**
```
item1: 600px × (1/4) = 150px
item2: 600px × (2/4) = 300px
item3: 600px × (1/4) = 150px
```

**结果：150px, 300px, 150px ✅**

**如果有 flex-basis：**
```css
.item1 { flex: 1 1 100px; }
.item2 { flex: 2 1 100px; }
.item3 { flex: 1 1 100px; }

/* 
  总basis: 300px
  剩余空间: 600 - 300 = 300px
  
  item1: 100 + 300×(1/4) = 175px
  item2: 100 + 300×(2/4) = 250px
  item3: 100 + 300×(1/4) = 175px
*/
```

</details>

---

## 第 9 题 🔴

**类型：** 代码题  
**标签：** 复杂布局

### 题目

以下代码中，`.item` 的对齐方式是？

```css
.container {
  display: flex;
  justify-content: center;
  align-items: flex-start;
}

.item {
  align-self: flex-end;
}
```

**选项：**
- A. 主轴居中，交叉轴起始
- B. 主轴居中，交叉轴末尾
- C. 主轴起始，交叉轴末尾
- D. 完全居中

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**align-self 覆盖 align-items**

**规则：**
- `justify-content` 作用于所有项目
- `align-items` 作用于所有项目
- `align-self` 覆盖单个项目的 `align-items`

**本题分析：**

**主轴（justify-content）：**
```css
justify-content: center;
/* 所有项目主轴居中 ✅ */
```

**交叉轴（align-items vs align-self）：**
```css
align-items: flex-start;  /* 默认：交叉轴起始 */
align-self: flex-end;     /* 覆盖：交叉轴末尾 ✅ */
```

**结果：**
- 主轴：居中（justify-content）
- 交叉轴：末尾（align-self 覆盖）

**可视化：**
```
交叉轴 ↑
      │
      │     item1  item2
      │              .item (align-self: end)
      │                    ↓
主轴 ─────────────────────→
      起始    居中    末尾
```

**优先级：**
```
align-self > align-items
```

</details>

---

## 第 10 题 🔴

**类型：** 多选题  
**标签：** FFC vs BFC

### 题目

FFC 和 BFC 的区别是？

**选项：**
- A. FFC 的 margin 不合并，BFC 内的 margin 会合并
- B. FFC 的 float 无效，BFC 中有效
- C. FFC 使用 flex 属性，BFC 不能
- D. FFC 是一维布局，BFC 是二维布局

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, C

### 📖 解析

**FFC vs BFC 对比**

**✅ A. Margin 合并**
```css
/* FFC：不合并 */
.flex {
  display: flex;
}
.item { margin: 10px; }
/* 间距 = 20px */

/* BFC：会合并 */
.block { margin: 10px; }
/* 间距 = 10px */
```

**✅ B. Float 效果**
```css
/* FFC：float 无效 */
.flex-item {
  float: left;  /* 被忽略 */
}

/* BFC：float 有效 */
.block {
  float: left;  /* 生效 */
}
```

**✅ C. Flex 属性**
```css
/* FFC：可用 */
.flex-item {
  flex: 1;  /* 有效 */
}

/* BFC：不可用 */
.block {
  flex: 1;  /* 无效 */
}
```

**❌ D. 维度（错误）**
```
FFC：一维布局（主轴方向）✅
BFC：也是管理块级盒的布局，不是"二维"❌

Grid 才是二维布局
```

**完整对比表：**

| 特性 | FFC | BFC |
|------|-----|-----|
| 创建方式 | flex/inline-flex | 多种方式 |
| 子元素排列 | 按主轴/交叉轴 | 按正常流 |
| margin合并 | ❌ 不合并 | ✅ 会合并 |
| float | ❌ 无效 | ✅ 有效 |
| flex属性 | ✅ 可用 | ❌ 不可用 |
| 对齐控制 | ✅ 强大 | ⚠️ 有限 |

</details>

---

**导航**  
[上一章：第 15 章 - IFC](./chapter-15.md) | [返回目录](../README.md) | [下一章：第 17 章 - GFC](./chapter-17.md)
