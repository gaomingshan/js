# 第 21 章：浮动与清除 - 面试题

**难度分布：** 🟢 简单 x3 | 🟡 中等 x4 | 🔴 困难 x3

---

## 第 1 题 🟢

**类型：** 单选题  
**标签：** float 基础

### 题目

`float` 属性的值有哪些？

**选项：**
- A. left, right
- B. left, right, none
- C. left, right, center
- D. left, right, none, inherit

<details>
<summary>查看答案</summary>

### ✅ 正确答案：D

### 📖 解析

**float 属性值**

```css
float: none;      /* 默认值，不浮动 */
float: left;      /* 左浮动 */
float: right;     /* 右浮动 */
float: inherit;   /* 继承父元素 */
```

**注意：**
- 没有 `float: center`
- CSS3 增加了 `inline-start` 和 `inline-end`（逻辑值）

**示例：**
```css
.left {
  float: left;
}

.right {
  float: right;
}
```

</details>

---

## 第 2 题 🟢

**类型：** 单选题  
**标签：** 浮动特性

### 题目

浮动元素的特点是？

**选项：**
- A. 脱离文档流
- B. 不占据空间
- C. 文字环绕
- D. 以上都是

<details>
<summary>查看答案</summary>

### ✅ 正确答案：D

### 📖 解析

**浮动元素特性（全部正确）**

**脱离文档流：**
```css
.float {
  float: left;
}
/* 脱离正常文档流 */
```

**不占据空间：**
```html
<div class="float">Float</div>
<div class="normal">Normal</div>
<!-- normal 会占据 float 的位置 -->
```

**文字环绕：**
```html
<img src="image.jpg" style="float: left;">
<p>文字会环绕图片...</p>
```

**可视化：**
```
┌────┐ 文字文字文字
│img │ 文字文字文字
└────┘ 文字文字文字
```

</details>

---

## 第 3 题 🟢

**类型：** 判断题  
**标签：** 高度塌陷

### 题目

父元素包含浮动子元素时，父元素高度会塌陷。

**选项：**
- A. ✅ 正确
- B. ❌ 错误

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A（正确）

### 📖 解析

**浮动导致高度塌陷**

```html
<div class="parent">
  <div class="float">Float Child</div>
</div>
```

```css
.float {
  float: left;
  height: 100px;
}

/* parent 高度为 0 ❌ */
```

**原因：**
- 浮动元素脱离文档流
- 父元素计算高度时不包括浮动子元素

**可视化：**
```
❌ 未清除浮动：
┌──────────────┐
├──────────────┤ parent (高度0)
  ┌─────┐
  │float│ 溢出
  └─────┘

✅ 清除浮动：
┌──────────────┐
│ ┌─────┐      │ parent (包含float)
│ │float│      │
│ └─────┘      │
└──────────────┘
```

</details>

---

## 第 4 题 🟡

**类型：** 多选题  
**标签：** 清除浮动

### 题目

以下哪些方法可以清除浮动？

**选项：**
- A. `clear: both`
- B. `overflow: hidden`
- C. `display: flow-root`
- D. 伪元素清除法

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, C, D

### 📖 解析

**清除浮动方法（全部正确）**

**✅ A. clear 属性**
```css
.clearfix::after {
  content: "";
  display: block;
  clear: both;
}
```

**✅ B. overflow（触发 BFC）**
```css
.parent {
  overflow: hidden;
}
```

**✅ C. flow-root（推荐）**
```css
.parent {
  display: flow-root;
}
```

**✅ D. 伪元素（经典）**
```css
.clearfix::after {
  content: "";
  display: table;
  clear: both;
}
```

**完整对比：**

**方法1：额外标签（不推荐）**
```html
<div class="parent">
  <div class="float">Float</div>
  <div style="clear: both;"></div>
</div>
```

**方法2：overflow（有副作用）**
```css
.parent {
  overflow: hidden;
  /* 副作用：裁剪溢出内容 */
}
```

**方法3：伪元素（经典）**
```css
.clearfix::before,
.clearfix::after {
  content: "";
  display: table;
}

.clearfix::after {
  clear: both;
}
```

**方法4：flow-root（推荐）**
```css
.parent {
  display: flow-root;
  /* 无副作用 ✅ */
}
```

</details>

---

## 第 5 题 🟡

**类型：** 代码题  
**标签：** clear 属性

### 题目

`clear: both` 的作用是？

**选项：**
- A. 清除自身浮动
- B. 清除父元素浮动
- C. 清除前面元素浮动的影响
- D. 清除所有浮动

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**clear 属性的作用**

```css
.clear {
  clear: both;
}
```

**含义：**
- 元素**不允许**前面有浮动元素
- 会移动到浮动元素下方

**示例：**
```html
<div class="float-left">Left</div>
<div class="float-right">Right</div>
<div class="clear">Clear</div>
```

```css
.float-left { float: left; }
.float-right { float: right; }
.clear { clear: both; }
```

**可视化：**
```
┌────┐         ┌─────┐
│Left│         │Right│
└────┘         └─────┘
┌────────────────────┐
│ Clear (下移)        │
└────────────────────┘
```

**clear 值：**
```css
clear: none;   /* 默认，允许浮动 */
clear: left;   /* 不允许左浮动 */
clear: right;  /* 不允许右浮动 */
clear: both;   /* 不允许任何浮动 */
```

**注意：**
- clear 作用于**自身**，不是父元素
- 用于清除**前面**浮动元素的影响

</details>

---

## 第 6 题 🟡

**类型：** 代码题  
**标签：** 浮动布局

### 题目

如何实现三列等宽浮动布局？

**选项：**
- A. 都设置 `float: left; width: 33.33%;`
- B. 都设置 `float: left; width: calc(100% / 3);`
- C. 第一个 `float: left`，其他不浮动
- D. A 和 B 都可以

<details>
<summary>查看答案</summary>

### ✅ 正确答案：D

### 📖 解析

**三列等宽布局**

**方法1：百分比**
```css
.col {
  float: left;
  width: 33.33%;  /* 或 33.333333% */
}
```

**方法2：calc()**
```css
.col {
  float: left;
  width: calc(100% / 3);
}
```

**完整示例：**
```html
<div class="container clearfix">
  <div class="col">Column 1</div>
  <div class="col">Column 2</div>
  <div class="col">Column 3</div>
</div>
```

```css
.container {
  display: flow-root;  /* 清除浮动 */
}

.col {
  float: left;
  width: 33.33%;
  box-sizing: border-box;
  padding: 10px;
}
```

**注意事项：**

**1. box-sizing**
```css
.col {
  width: 33.33%;
  padding: 10px;
  box-sizing: border-box;  /* 包含 padding */
}
```

**2. 精度问题**
```css
/* 可能有1px误差 */
width: 33.33%;

/* 更精确 */
width: calc(100% / 3);

/* 或使用 flexbox */
display: flex;
.col { flex: 1; }
```

**3. 间距处理**
```css
/* 负 margin */
.container {
  margin: 0 -10px;
}
.col {
  padding: 0 10px;
}
```

</details>

---

## 第 7 题 🟡

**类型：** 单选题  
**标签：** 浮动影响

### 题目

浮动元素会影响哪些元素？

**选项：**
- A. 后面的块级元素
- B. 后面的行内元素
- C. 父元素
- D. 以上都是

<details>
<summary>查看答案</summary>

### ✅ 正确答案：D

### 📖 解析

**浮动的影响范围**

**影响后面的块级元素：**
```html
<div class="float">Float</div>
<div class="block">Block</div>
```

```css
.float { float: left; width: 100px; }
/* block 会占据 float 的位置 */
```

**影响后面的行内元素：**
```html
<div class="float">Float</div>
<span>Text</span>
```

```css
/* Text 会环绕 float */
```

**影响父元素：**
```html
<div class="parent">
  <div class="float">Float</div>
</div>
```

```css
/* parent 高度塌陷 */
```

**完整示例：**
```html
<div class="container">
  <img src="img.jpg" class="float-left">
  <h2>标题</h2>
  <p>段落文字会环绕图片...</p>
</div>
```

```css
.float-left {
  float: left;
  margin-right: 10px;
}
/* 标题和段落都会环绕图片 */
```

</details>

---

## 第 8 题 🔴

**类型：** 代码题  
**标签：** 浮动顺序

### 题目

以下代码的布局结果是？

```html
<div class="container">
  <div class="a">A</div>
  <div class="b">B</div>
  <div class="c">C</div>
</div>
```

```css
.container { width: 300px; }
.a { float: left; width: 150px; }
.b { float: right; width: 150px; }
.c { float: left; width: 150px; }
```

**选项：**
- A. A B C 横向排列
- B. A C 在第一行，B 在第二行
- C. A 在左，B 在右，C 在下一行左侧
- D. 三个都在同一行

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**浮动元素的排列规则**

**第一行：**
```
A (float: left, 150px)   B (float: right, 150px)
├──────────────┤├──────────────┤
     150px            150px
容器宽度：300px ✅ 刚好
```

**第二行：**
```
C (float: left, 150px)
├──────────────┤
     150px
```

**可视化：**
```
┌─────────────────────────┐
│ ┌────┐         ┌────┐  │
│ │ A  │         │ B  │  │
│ └────┘         └────┘  │
│ ┌────┐                 │
│ │ C  │                 │
│ └────┘                 │
└─────────────────────────┘
```

**关键点：**
1. A 先浮动到左侧
2. B 浮动到右侧（同一行）
3. C 浮动到左侧，但第一行没有足够空间
4. C 换到第二行左侧

**如果顺序不同：**
```html
<div class="a">A</div>
<div class="c">C</div>
<div class="b">B</div>
```

```
┌─────────────────────────┐
│ ┌────┐ ┌────┐          │
│ │ A  │ │ C  │          │
│ └────┘ └────┘          │
│                 ┌────┐ │
│                 │ B  │ │
│                 └────┘ │
└─────────────────────────┘
```

</details>

---

## 第 9 题 🔴

**类型：** 代码题  
**标签：** 浮动与 BFC

### 题目

如何防止元素被浮动元素覆盖？

```html
<div class="float">Float</div>
<div class="content">Content</div>
```

**选项：**
- A. content 添加 `clear: both`
- B. content 触发 BFC
- C. content 也浮动
- D. 以上都可以

<details>
<summary>查看答案</summary>

### ✅ 正确答案：D

### 📖 解析

**防止浮动覆盖的方法**

**方法1：clear（清除）**
```css
.content {
  clear: both;
}
/* 移动到浮动元素下方 */
```

**方法2：BFC（推荐）**
```css
.content {
  overflow: hidden;  /* 或 flow-root */
}
/* 不与浮动元素重叠 */
```

**方法3：浮动**
```css
.content {
  float: left;
}
/* 自身也浮动 */
```

**效果对比：**

**❌ 未处理：**
```
┌────┐
│Float─┐
└────┘ │ Content (被覆盖)
       └────────────────
```

**✅ clear: both：**
```
┌────┐
│Float│
└────┘
┌──────────────────┐
│ Content (下移)    │
└──────────────────┘
```

**✅ BFC (overflow)：**
```
┌────┐┌──────────┐
│Float││ Content  │
│     ││ (避开)   │
└────┘└──────────┘
```

**最佳实践：**

**两栏布局（侧边栏 + 内容）：**
```css
.sidebar {
  float: left;
  width: 200px;
}

.main {
  overflow: hidden;  /* BFC，自适应宽度 */
  /* 或 */
  margin-left: 220px;  /* 固定边距 */
}
```

</details>

---

## 第 10 题 🔴

**类型：** 多选题  
**标签：** 浮动问题

### 题目

关于浮动的说法，正确的是？

**选项：**
- A. 浮动元素会自动变为块级元素
- B. 浮动元素的 margin 不会合并
- C. 浮动元素可以设置宽高
- D. 现代布局应优先使用 flexbox/grid

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, C, D

### 📖 解析

**浮动特性总结（全部正确）**

**✅ A. 变为块级**
```css
span {
  float: left;
  width: 100px;  /* 可以设置 ✅ */
  /* display 计算值变为 block */
}
```

**✅ B. margin 不合并**
```css
.float1 { float: left; margin-right: 10px; }
.float2 { float: left; margin-left: 10px; }
/* 间距 = 20px，不合并 */
```

**✅ C. 可设置宽高**
```css
.float {
  float: left;
  width: 200px;   ✅
  height: 100px;  ✅
}
```

**✅ D. 优先现代布局**
```css
/* ❌ 旧方式：浮动 */
.col {
  float: left;
  width: 33.33%;
}

/* ✅ 推荐：flexbox */
.container {
  display: flex;
}
.col {
  flex: 1;
}

/* ✅ 推荐：grid */
.container {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
}
```

**浮动的局限性：**

**1. 高度塌陷**
```css
/* 需要清除浮动 */
.clearfix::after {
  content: "";
  display: table;
  clear: both;
}
```

**2. 布局复杂**
```css
/* 多列等高困难 */
/* 垂直居中困难 */
/* 响应式布局复杂 */
```

**现代替代方案：**

**Flexbox（一维布局）：**
```css
.flex-container {
  display: flex;
  gap: 20px;
}

.flex-item {
  flex: 1;
}
```

**Grid（二维布局）：**
```css
.grid-container {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 20px;
}
```

**何时仍使用浮动：**
- 文字环绕图片
- 兼容老浏览器
- 简单的左右布局

</details>

---

**导航**  
[上一章：第 20 章 - 定位详解](./chapter-20.md) | [返回目录](../README.md) | [下一章：第 22 章 - Flexbox 基础](./chapter-22.md)
