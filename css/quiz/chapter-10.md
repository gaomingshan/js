# 第 10 章：单位与值转换 - 面试题

**难度分布：** 🟢 简单 x3 | 🟡 中等 x4 | 🔴 困难 x3

---

## 第 1 题 🟢

**类型：** 单选题  
**标签：** 相对单位

### 题目

`em` 和 `rem` 的主要区别是？

**选项：**
- A. em 相对父元素，rem 相对根元素
- B. em 是绝对单位，rem 是相对单位
- C. em 用于字体，rem 用于布局
- D. 没有区别

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A

### 📖 解析

**em vs rem**

**em - 相对父元素**
```css
.parent { font-size: 20px; }
.child {
  font-size: 1.5em;  /* 1.5 × 20px = 30px */
  padding: 1em;      /* 1 × 30px = 30px（相对自己）*/
}
```

**rem - 相对根元素**
```css
html { font-size: 16px; }
.box {
  font-size: 1.5rem;  /* 1.5 × 16px = 24px */
  padding: 1rem;      /* 1 × 16px = 16px */
}
```

**对比：**

**em 的继承**
```css
html { font-size: 16px; }
.level1 { font-size: 1.5em; }  /* 24px */
.level2 { font-size: 1.5em; }  /* 36px（1.5 × 24）*/
.level3 { font-size: 1.5em; }  /* 54px（1.5 × 36）*/
```

**rem 的稳定性**
```css
html { font-size: 16px; }
.level1 { font-size: 1.5rem; }  /* 24px */
.level2 { font-size: 1.5rem; }  /* 24px */
.level3 { font-size: 1.5rem; }  /* 24px */
```

**使用场景：**
```css
/* em：组件内部间距 */
.button {
  font-size: 16px;
  padding: 0.5em 1em;  /* 8px 16px，跟随字体大小 */
}

/* rem：全局尺寸 */
.container {
  width: 60rem;  /* 960px（基于根元素）*/
  margin: 2rem auto;
}
```

</details>

---

## 第 2 题 🟢

**类型：** 单选题  
**标签：** 视口单位

### 题目

`1vw` 等于什么？

**选项：**
- A. 视口宽度的 1%
- B. 视口高度的 1%
- C. 1 像素
- D. 根元素字体大小的 1%

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A

### 📖 解析

**视口单位详解**

**vw - 视口宽度**
```css
.box {
  width: 50vw;  /* 视口宽度的 50% */
}

/* 视口宽度 1000px */
/* 50vw = 500px */
```

**vh - 视口高度**
```css
.box {
  height: 100vh;  /* 视口高度的 100% */
}
```

**vmin/vmax**
```css
.box {
  width: 50vmin;  /* min(50vw, 50vh) */
  height: 50vmax; /* max(50vw, 50vh) */
}
```

**实用场景：**

**1. 全屏布局**
```css
.hero {
  width: 100vw;
  height: 100vh;
}
```

**2. 响应式字体**
```css
h1 {
  font-size: calc(2rem + 2vw);
  /* 基础 32px + 响应式调整 */
}
```

**3. 居中定位**
```css
.modal {
  position: fixed;
  left: 50vw;
  top: 50vh;
  transform: translate(-50%, -50%);
}
```

**注意事项：**
```css
/* ⚠️ 移动端滚动条 */
width: 100vw;  /* 可能包含滚动条宽度 */

/* ✅ 更好的方案 */
width: 100%;   /* 不包含滚动条 */
```

</details>

---

## 第 3 题 🟢

**类型：** 判断题  
**标签：** 百分比

### 题目

padding 和 margin 的百分比值都是相对于父元素的宽度。

**选项：**
- A. ✅ 正确
- B. ❌ 错误

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A（正确）

### 📖 解析

**百分比相对值规则**

**padding/margin 百分比 → 父元素宽度**

```css
.parent {
  width: 1000px;
  height: 500px;
}

.child {
  padding-top: 10%;     /* 100px（相对宽度！）*/
  padding-left: 10%;    /* 100px */
  margin-bottom: 5%;    /* 50px（相对宽度！）*/
}
```

**为什么都相对宽度？**
- 避免循环依赖
- 确保计算的确定性
- CSS 规范规定

**实用技巧：固定宽高比**
```css
.aspect-box {
  width: 100%;
  padding-bottom: 56.25%;  /* 16:9 比例 */
  position: relative;
}

.content {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}
```

**其他百分比：**

**width/height**
```css
width: 50%;   /* 相对父元素宽度 */
height: 50%;  /* 相对父元素高度 */
```

**transform**
```css
transform: translateX(50%);  /* 相对自身宽度 */
transform: translateY(50%);  /* 相对自身高度 */
```

**font-size**
```css
font-size: 120%;  /* 相对父元素 font-size */
```

</details>

---

## 第 4 题 🟡

**类型：** 代码题  
**标签：** calc() 函数

### 题目

`calc(100% - 50px)` 的计算发生在哪个阶段？

**选项：**
- A. 声明值阶段
- B. 计算值阶段
- C. 使用值阶段
- D. 实际值阶段

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B, C

### 📖 解析

**calc() 的计算时机**

**分阶段计算：**
```css
.box {
  width: calc(100% - 50px);
}
```

**计算值阶段：**
```css
/* 解析表达式，转换单位 */
width: calc(100% - 50px);
/* 计算值：calc(100% - 50px)（保留%）*/
```

**使用值阶段：**
```css
/* 父元素 1000px */
/* 100% = 1000px */
/* 1000px - 50px = 950px */
/* 使用值：950px ✅ */
```

**详细过程：**

**阶段1：词法分析**
```
calc(100% - 50px)
→ 函数调用
→ 参数：100% - 50px
```

**阶段2：计算值**
```
100%  → 保留（需要父元素信息）
50px  → 50px
calc表达式 → 保留
```

**阶段3：使用值**
```
父元素：1000px
100% → 1000px
计算：1000px - 50px = 950px ✅
```

**混合单位：**
```css
/* em + px */
width: calc(10em + 50px);
/* 计算值：calc(160px + 50px) = 210px */

/* % + vw */
width: calc(50% + 10vw);
/* 计算值：calc(50% + 10vw) */
/* 使用值：根据视口和父元素计算 */
```

**实用示例：**
```css
/* 响应式间距 */
.container {
  width: calc(100% - 40px);
  margin: 0 20px;
}

/* 减去固定区域 */
.main {
  width: calc(100vw - 250px);  /* 视口 - 侧边栏 */
}

/* 复杂计算 */
.box {
  width: calc((100% - 60px) / 3);  /* 三栏，间距20px */
}
```

</details>

---

## 第 5 题 🟡

**类型：** 多选题  
**标签：** 数学函数

### 题目

CSS 中的数学函数有哪些？

**选项：**
- A. `calc()`
- B. `min()`
- C. `max()`
- D. `clamp()`

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, C, D

### 📖 解析

**CSS 数学函数（全部正确）**

**✅ A. calc() - 计算**
```css
.box {
  width: calc(100% - 50px);
  height: calc(100vh - 60px);
  margin: calc(1rem + 10px);
}
```

**✅ B. min() - 最小值**
```css
.box {
  width: min(100%, 500px);
  /* 取较小值 */
}
```

**✅ C. max() - 最大值**
```css
.box {
  width: max(200px, 50%);
  /* 取较大值 */
}
```

**✅ D. clamp() - 限制范围**
```css
.box {
  width: clamp(200px, 50%, 800px);
  /* min: 200px, 首选: 50%, max: 800px */
}
```

**实用场景：**

**1. 响应式字体**
```css
h1 {
  font-size: clamp(1.5rem, 2vw + 1rem, 3rem);
  /* 最小 24px，最大 48px，中间响应式 */
}
```

**2. 容器限制**
```css
.container {
  width: min(100% - 40px, 1200px);
  /* 不超过 1200px，且留 40px 边距 */
}
```

**3. 间距计算**
```css
.grid {
  gap: max(1rem, 2vw);
  /* 至少 16px，响应式增大 */
}
```

**4. 复杂布局**
```css
.sidebar {
  width: clamp(200px, 20vw, 300px);
}

.main {
  width: calc(100% - clamp(200px, 20vw, 300px) - 20px);
}
```

**嵌套使用：**
```css
.box {
  width: clamp(
    200px,
    calc(50% - 20px),
    max(400px, 40vw)
  );
}
```

</details>

---

## 第 6 题 🟡

**类型：** 代码题  
**标签：** clamp() 函数

### 题目

`clamp(100px, 50%, 400px)` 在父元素宽度为 600px 时等于多少？

**选项：**
- A. 100px
- B. 300px
- C. 400px
- D. 600px

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**clamp() 计算规则**

**语法：`clamp(min, val, max)`**
```css
clamp(100px, 50%, 400px)
```

**计算过程：**
```
父元素：600px
50% = 300px

min: 100px
val: 300px
max: 400px

比较：
300px >= 100px ✅
300px <= 400px ✅

结果：300px ✅
```

**其他情况：**

**情况1：值太小**
```css
/* 父元素：100px */
clamp(100px, 50%, 400px)
/* 50% = 50px */
/* 50px < 100px → 使用 min: 100px */
```

**情况2：值太大**
```css
/* 父元素：1000px */
clamp(100px, 50%, 400px)
/* 50% = 500px */
/* 500px > 400px → 使用 max: 400px */
```

**等价写法：**
```css
/* clamp(min, val, max) */
clamp(100px, 50%, 400px)

/* = */
max(100px, min(50%, 400px))
```

**实用示例：**

**响应式宽度**
```css
.container {
  width: clamp(320px, 90%, 1200px);
  /* 
    小屏：320px
    中屏：90%（响应式）
    大屏：1200px
  */
}
```

**响应式字体**
```css
h1 {
  font-size: clamp(1.5rem, 2vw + 1rem, 3rem);
}

/* 计算示例：
   视口 400px：2vw = 8px
   → 8px + 16px = 24px = 1.5rem（min）
   
   视口 800px：2vw = 16px
   → 16px + 16px = 32px = 2rem（中间值）
   
   视口 1600px：2vw = 32px
   → 32px + 16px = 48px = 3rem（max）
*/
```

</details>

---

## 第 7 题 🟡

**类型：** 单选题  
**标签：** 容器查询单位

### 题目

`cqw` 单位相对于什么？

**选项：**
- A. 视口宽度
- B. 根元素宽度
- C. 容器宽度
- D. 父元素宽度

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**容器查询单位（Container Query Units）**

**cqw - 容器宽度**
```css
.container {
  container-type: inline-size;
  width: 800px;
}

.item {
  width: 50cqw;  /* 400px（容器宽度的 50%）*/
}
```

**容器查询单位列表：**
```css
cqw   /* 容器宽度的 1% */
cqh   /* 容器高度的 1% */
cqi   /* 容器 inline-size 的 1% */
cqb   /* 容器 block-size 的 1% */
cqmin /* min(cqi, cqb) */
cqmax /* max(cqi, cqb) */
```

**设置容器：**
```css
.container {
  container-type: inline-size;  /* 或 size */
  container-name: sidebar;      /* 可选命名 */
}
```

**使用示例：**
```css
.card {
  padding: 2cqw;           /* 响应容器大小 */
  font-size: clamp(1rem, 3cqw, 2rem);
}
```

**对比 vw：**
```css
/* vw - 相对视口 */
.box { width: 50vw; }  /* 视口宽度的 50% */

/* cqw - 相对容器 */
.box { width: 50cqw; }  /* 容器宽度的 50% */
```

**完整示例：**
```css
.sidebar {
  container-type: inline-size;
  width: 300px;
}

.card {
  /* 响应 sidebar 宽度 */
  padding: 3cqw;           /* 9px */
  font-size: 4cqw;         /* 12px */
  border-radius: 2cqw;     /* 6px */
}

/* 当 sidebar 变为 600px */
.card {
  padding: 18px;          /* 3% × 600 */
  font-size: 24px;        /* 4% × 600 */
  border-radius: 12px;    /* 2% × 600 */
}
```

</details>

---

## 第 8 题 🔴

**类型：** 代码题  
**标签：** 单位转换

### 题目

以下代码中，`<div>` 的最终高度（像素）是多少？

```css
html { font-size: 16px; }
body {
  font-size: 1.25rem;
  line-height: 1.5;
}
div {
  font-size: 1.2em;
  height: 3em;
}
```

**选项：**
- A. 48px
- B. 57.6px
- C. 72px
- D. 86.4px

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**复杂单位转换**

**逐步计算：**

**1. html**
```css
html { font-size: 16px; }
/* 基准：16px */
```

**2. body**
```css
body { font-size: 1.25rem; }
/* 1.25 × 16px = 20px */
```

**3. div 的 font-size**
```css
div { font-size: 1.2em; }
/* 1.2 × 20px（父元素）= 24px */
```

**4. div 的 height**
```css
div { height: 3em; }
/* 3 × 24px（自身 font-size）= 72px ✅ */
```

**关键点：**

**rem 计算**
- 始终相对根元素（html）
- body { 1.25rem } = 1.25 × 16 = 20px

**em 计算**
- font-size 相对父元素
- 其他属性相对自身 font-size

**完整计算链：**
```
html: 16px
  ↓
body: 1.25rem = 20px
  ↓
div font-size: 1.2em = 24px
  ↓
div height: 3em = 72px ✅
```

**如果换成 rem：**
```css
div {
  font-size: 1.2rem;  /* 19.2px（1.2 × 16）*/
  height: 3rem;       /* 48px（3 × 16）*/
}
```

**line-height 不影响：**
```css
body { line-height: 1.5; }
/* 只影响行高，不影响 em 计算 */
```

</details>

---

## 第 9 题 🔴

**类型：** 代码题  
**标签：** 百分比陷阱

### 题目

以下代码中，`.child` 的高度是多少？

```css
.parent {
  height: auto;
}

.child {
  height: 50%;
}
```

**选项：**
- A. 0
- B. 父元素高度的 50%
- C. 视口高度的 50%
- D. auto

<details>
<summary>查看答案</summary>

### ✅ 正确答案：D

### 📖 解析

**百分比高度的陷阱**

**规则：父元素高度为 auto 时，子元素百分比高度无效**

```css
.parent {
  height: auto;  /* 或未设置高度 */
}

.child {
  height: 50%;   /* 无效 → auto ✅ */
}
```

**原因：**
- 子元素百分比依赖父元素高度
- 父元素 auto 依赖子元素内容
- 形成循环依赖

**解决方案：**

**方案1：设置父元素高度**
```css
.parent {
  height: 500px;  /* 明确高度 */
}

.child {
  height: 50%;    /* 250px ✅ */
}
```

**方案2：使用 vh**
```css
.child {
  height: 50vh;   /* 视口高度的 50% */
}
```

**方案3：flexbox**
```css
.parent {
  display: flex;
  flex-direction: column;
  min-height: 500px;
}

.child {
  flex: 1;        /* 自动填充 */
}
```

**方案4：绝对定位**
```css
.parent {
  position: relative;
  height: auto;
}

.child {
  position: absolute;
  top: 0;
  bottom: 0;
  height: 50%;    /* 此时有效 */
}
```

**对比宽度：**
```css
.parent {
  width: auto;    /* 默认 */
}

.child {
  width: 50%;     /* 有效 ✅ */
}
/* 宽度不依赖子元素，不形成循环 */
```

**特殊情况：**
```css
html, body {
  height: 100%;   /* 设置根高度 */
}

.container {
  height: 100%;   /* 继承 */
}

.child {
  height: 50%;    /* 50vh ✅ */
}
```

</details>

---

## 第 10 题 🔴

**类型：** 多选题  
**标签：** 单位最佳实践

### 题目

关于 CSS 单位的最佳实践，以下说法正确的是？

**选项：**
- A. 字体大小使用 rem
- B. 间距使用 em 保持相对性
- C. 媒体查询使用 em 或 rem
- D. 边框使用 px

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, C, D

### 📖 解析

**CSS 单位最佳实践（全部正确）**

**✅ A. 字体使用 rem**
```css
html { font-size: 16px; }

body { font-size: 1rem; }      /* 16px */
h1 { font-size: 2rem; }        /* 32px */
small { font-size: 0.875rem; } /* 14px */
```

**好处：**
- 全局统一缩放
- 用户设置字体大小生效
- 易于维护

**✅ B. 间距使用 em**
```css
.button {
  font-size: 16px;
  padding: 0.5em 1em;     /* 8px 16px */
  margin-bottom: 1em;     /* 16px */
}

.button.large {
  font-size: 20px;
  /* padding 自动变为 10px 20px */
}
```

**好处：**
- 跟随组件字体大小
- 保持比例关系

**✅ C. 媒体查询使用 em/rem**
```css
/* ✅ 推荐 */
@media (min-width: 48em) {  /* 768px */
  .container { width: 750px; }
}

/* ❌ 避免 */
@media (min-width: 768px) {
  /* 用户缩放时不响应 */
}
```

**好处：**
- 尊重用户字体设置
- 更好的可访问性

**✅ D. 边框使用 px**
```css
.box {
  border: 1px solid #ddd;
  /* 边框通常固定像素 */
}
```

**原因：**
- 1px 在任何缩放下清晰
- 边框不需要响应式

**完整示例：**
```css
/* 根字体 */
html {
  font-size: 16px;
}

/* 响应式调整 */
@media (min-width: 48em) {
  html { font-size: 18px; }
}

/* 组件 */
.card {
  font-size: 1rem;           /* 16px → 18px */
  padding: 1em;              /* 16px → 18px */
  margin-bottom: 1.5rem;     /* 24px → 27px */
  border: 1px solid #ddd;    /* 1px（固定）*/
  border-radius: 0.25rem;    /* 4px → 4.5px */
}

/* 布局 */
.container {
  max-width: 75rem;          /* 1200px → 1350px */
  padding: 0 1rem;
}
```

**其他最佳实践：**

**1. 百分比布局**
```css
.col-half { width: 50%; }
.col-third { width: 33.333%; }
```

**2. vw/vh 全屏**
```css
.hero {
  height: 100vh;
  width: 100vw;
}
```

**3. ch 单位（等宽字体）**
```css
input {
  width: 20ch;  /* 容纳 20 个字符 */
}
```

**4. 混合使用**
```css
.responsive {
  width: clamp(200px, 50%, 800px);
  padding: max(1rem, 2vw);
  font-size: calc(1rem + 0.5vw);
}
```

</details>

---

**导航**  
[上一章：第 9 章 - 样式值计算过程](./chapter-09.md) | [返回目录](../README.md) | [下一章：第 11 章 - 包含块](./chapter-11.md)
