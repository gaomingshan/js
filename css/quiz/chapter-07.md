# 第 7 章：层叠算法详解 - 面试题

**难度分布：** 🟢 简单 x3 | 🟡 中等 x4 | 🔴 困难 x3

---

## 第 1 题 🟢

**类型：** 单选题  
**标签：** 层叠来源

### 题目

CSS 层叠的来源优先级从高到低是？

**选项：**
- A. 作者样式 > 用户样式 > 浏览器默认
- B. 浏览器默认 > 用户样式 > 作者样式
- C. 用户样式 > 作者样式 > 浏览器默认
- D. 优先级相同

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A

### 📖 解析

**层叠来源优先级（不含!important）**

```
1. 作者样式（开发者写的CSS）      最高 ✅
2. 用户样式（用户自定义样式）     中等
3. 浏览器默认样式（User Agent）   最低
```

**示例：**
```css
/* 浏览器默认 */
a { color: blue; }

/* 用户样式 */
a { color: purple; }

/* 作者样式 - 优先级最高 */
a { color: red; }  /* 最终显示红色 ✅ */
```

**带 !important 时优先级反转：**
```
1. 用户 !important              最高
2. 作者 !important              中等
3. 作者样式                     
4. 用户样式                     
5. 浏览器默认                   最低
```

**完整优先级顺序：**
```
1. 过渡动画（transition）
2. 用户 !important
3. 作者 !important
4. 作者样式
5. 用户样式
6. 浏览器默认
```

</details>

---

## 第 2 题 🟢

**类型：** 单选题  
**标签：** !important

### 题目

`!important` 的作用是？

**选项：**
- A. 删除样式规则
- B. 提升该声明的优先级
- C. 标记样式为关键样式
- D. 使样式不可覆盖

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**!important 详解**

**作用：提升优先级到最高级别**

```css
.box {
  color: red !important;  /* 优先级极高 */
}

#box {
  color: blue;  /* ID选择器也无法覆盖 */
}
/* 最终：red ✅ */
```

**优先级对比：**
```css
/* 普通声明 */
.box { color: red; }        /* 优先级：10 */
#box { color: blue; }       /* 优先级：100 */

/* 带 !important */
.box { color: red !important; }  /* 优先级：极高 */
#box { color: blue; }            /* 无法覆盖 */
```

**唯一能覆盖的方式：**
```css
.box { color: red !important; }
#box { color: blue !important; }  /* 更高特异性的 !important */
/* 最终：blue ✅ */
```

**语法位置：**
```css
/* ✅ 正确 */
color: red !important;

/* ❌ 错误 */
!important color: red;
color: !important red;
```

**最佳实践：**
```css
/* ❌ 避免滥用 */
.box { color: red !important; }

/* ✅ 只在必要时使用 */
.utility-class { display: none !important; }  /* 工具类 */
.js-override { color: red !important; }       /* JS无法修改时 */
```

</details>

---

## 第 3 题 🟢

**类型：** 判断题  
**标签：** 层叠规则

### 题目

相同选择器的样式规则，后定义的会覆盖先定义的。

**选项：**
- A. ✅ 正确
- B. ❌ 错误

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A（正确）

### 📖 解析

**层叠顺序规则**

**后定义覆盖先定义（特异性相同时）**

```css
.box { color: red; }   /* 先定义 */
.box { color: blue; }  /* 后定义 - 覆盖 */
/* 最终：blue ✅ */
```

**完整层叠规则：**
```
1. 比较来源（作者 > 用户 > 浏览器）
2. 比较特异性（ID > 类 > 元素）
3. 比较顺序（后定义 > 先定义）
```

**示例分析：**

**情况1：特异性相同**
```css
.box { color: red; }
.box { color: blue; }
/* 顺序决定：blue ✅ */
```

**情况2：特异性不同**
```css
.box { color: blue; }    /* 后定义，但特异性低 */
#box { color: red; }     /* 先定义，但特异性高 */
/* 特异性决定：red ✅ */
```

**情况3：不同文件**
```html
<link rel="stylesheet" href="a.css">  <!-- .box { color: red; } -->
<link rel="stylesheet" href="b.css">  <!-- .box { color: blue; } -->
<!-- 最终：blue ✅ -->
```

**情况4：内联样式**
```html
<div class="box" style="color: green;">
<!-- 内联样式优先级最高：green ✅ -->
</div>
```

</details>

---

## 第 4 题 🟡

**类型：** 代码题  
**标签：** 特异性计算

### 题目

以下选择器的特异性是多少？

```css
div#header.nav > li:first-child::before
```

**选项：**
- A. (1, 1, 3)
- B. (1, 2, 3)
- C. (1, 2, 2)
- D. (0, 3, 3)

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**特异性计算规则**

**格式：(a, b, c)**
- a: ID选择器数量
- b: 类/属性/伪类选择器数量
- c: 元素/伪元素选择器数量

**本题分解：**
```css
div#header.nav > li:first-child::before
│   │      │    │  │           │
│   │      │    │  │           └─ 伪元素 +1c
│   │      │    │  └─────────── 伪类 +1b
│   │      │    └────────────── 元素 +1c
│   │      └─────────────────── 类 +1b
│   └────────────────────────── ID +1a
└────────────────────────────── 元素 +1c
```

**计算结果：**
```
ID选择器：    1个  (#header)
类/伪类：     2个  (.nav, :first-child)
元素/伪元素： 3个  (div, li, ::before)

特异性：(1, 2, 3)
```

**更多示例：**
```css
#id                          /* (1, 0, 0) */
.class                       /* (0, 1, 0) */
div                          /* (0, 0, 1) */
#id.class div                /* (1, 1, 1) */
div.class[type="text"]       /* (0, 2, 1) */
#header .nav li:hover        /* (1, 2, 1) */
```

**比较特异性：**
```
(1, 0, 0) > (0, 10, 0)  /* ID > 10个类 */
(0, 2, 0) > (0, 1, 10)  /* 2个类 > 1个类+10个元素 */
```

</details>

---

## 第 5 题 🟡

**类型：** 多选题  
**标签：** 层叠层

### 题目

CSS `@layer` 规则的特点是？

**选项：**
- A. 可以控制层叠顺序
- B. 层级内部仍遵循正常层叠规则
- C. 后定义的层优先级更高
- D. 可以嵌套层级

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, C, D

### 📖 解析

**@layer 层叠层（全部正确）**

**✅ A. 控制层叠顺序**
```css
@layer reset, base, components, utilities;

@layer reset {
  * { margin: 0; }  /* 优先级最低 */
}

@layer utilities {
  .mt-0 { margin-top: 0; }  /* 优先级最高 */
}
```

**✅ B. 层内正常规则**
```css
@layer components {
  #id { color: red; }     /* ID优先级高 */
  .class { color: blue; }
}
```

**✅ C. 后定义层优先级高**
```css
@layer A {
  .box { color: red; }
}

@layer B {
  .box { color: blue; }  /* 优先级更高 */
}
/* 最终：blue ✅ */
```

**✅ D. 嵌套层级**
```css
@layer framework {
  @layer base {
    p { margin: 0; }
  }
  
  @layer components {
    .card { padding: 20px; }
  }
}
```

**声明顺序：**
```css
/* 先声明层顺序 */
@layer reset, base, theme, components, utilities;

/* 后填充内容 */
@layer base {
  /* 基础样式 */
}

@layer components {
  /* 组件样式 */
}
```

**优先级规则：**
```
无层样式 > 有层样式
后定义层 > 先定义层
层内：正常层叠规则
```

**实用场景：**
```css
/* 重置样式 */
@layer reset {
  * { box-sizing: border-box; }
}

/* 基础样式 */
@layer base {
  body { font-size: 16px; }
}

/* 组件 */
@layer components {
  .btn { padding: 10px; }
}

/* 工具类（最高优先级）*/
@layer utilities {
  .hidden { display: none; }
}
```

</details>

---

## 第 6 题 🟡

**类型：** 代码题  
**标签：** 层叠冲突

### 题目

以下代码中，`.box` 的颜色是？

```css
.container .box { color: red; }
#container > .box { color: blue; }
.box { color: green !important; }
```

```html
<div id="container" class="container">
  <div class="box">Text</div>
</div>
```

**选项：**
- A. red
- B. blue
- C. green
- D. 继承父元素

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**层叠优先级分析**

**特异性计算：**
```css
/* 1. .container .box */
/*    0个ID + 2个类 + 0个元素 = (0,2,0) */
.container .box { color: red; }

/* 2. #container > .box */
/*    1个ID + 1个类 + 0个元素 = (1,1,0) */
#container > .box { color: blue; }

/* 3. .box !important */
/*    !important 优先级最高 */
.box { color: green !important; }
```

**优先级对比：**
```
!important > 任何特异性

green !important (最高)
  ↓
(1,1,0) = blue
  ↓
(0,2,0) = red
```

**最终结果：green ✅**

**原因：**
- `!important` 优先级最高
- 即使特异性很低（0,1,0）
- 也能覆盖高特异性规则

**如果没有 !important：**
```css
.container .box { color: red; }    /* (0,2,0) */
#container > .box { color: blue; } /* (1,1,0) 胜出 */
.box { color: green; }             /* (0,1,0) */

/* 最终：blue */
```

**完整优先级顺序：**
```
1. 内联 !important
2. 作者 !important（按特异性）
3. 内联样式
4. 作者样式（按特异性）
5. 浏览器默认
```

</details>

---

## 第 7 题 🟡

**类型：** 单选题  
**标签：** 继承与层叠

### 题目

继承的样式和直接指定的样式冲突时？

**选项：**
- A. 继承优先
- B. 直接指定优先
- C. 取决于特异性
- D. 两者都应用

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**继承 vs 直接指定**

**直接指定始终优先于继承**

```css
body {
  color: red;  /* 继承给所有子元素 */
}

p {
  color: blue;  /* 直接指定 - 优先 */
}
```

```html
<body>
  <p>蓝色文字</p>  <!-- blue ✅ -->
</body>
```

**优先级规则：**
```
直接指定（任何选择器）> 继承
```

**即使是通配符：**
```css
body {
  color: red;
}

* {
  color: blue;  /* 通配符 > 继承 */
}
```

**特异性为0也优先：**
```css
.parent {
  color: red;
}

* {
  color: blue;  /* (0,0,0) 但仍优先于继承 */
}
```

**完整优先级：**
```
1. !important
2. 内联样式
3. ID选择器
4. 类选择器
5. 元素选择器
6. 通配符
7. 继承        ← 最低
8. 浏览器默认
```

**示例对比：**
```css
/* 场景1 */
.parent { color: red; }
.child { }  /* 继承 red */

/* 场景2 */
.parent { color: red; }
* { color: blue; }
.child { }  /* blue（通配符 > 继承）*/

/* 场景3 */
.parent { color: red; }
.child { color: blue; }  /* blue（直接指定）*/
```

</details>

---

## 第 8 题 🔴

**类型：** 代码题  
**标签：** 复杂层叠

### 题目

分析以下代码，`.target` 的 `font-size` 是多少？

```html
<div id="app" class="container">
  <div class="wrapper">
    <p class="text target">Content</p>
  </div>
</div>
```

```css
#app .target { font-size: 16px; }
.container .text { font-size: 18px !important; }
div div p { font-size: 20px; }
.target { font-size: 14px !important; }
```

**选项：**
- A. 14px
- B. 16px
- C. 18px
- D. 20px

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**复杂层叠分析**

**特异性计算：**
```css
/* 1. #app .target */
/*    1个ID + 1个类 = (1,1,0) + 无!important */
#app .target { font-size: 16px; }

/* 2. .container .text */
/*    2个类 = (0,2,0) + !important */
.container .text { font-size: 18px !important; }

/* 3. div div p */
/*    3个元素 = (0,0,3) + 无!important */
div div p { font-size: 20px; }

/* 4. .target */
/*    1个类 = (0,1,0) + !important */
.target { font-size: 14px !important; }
```

**优先级对比：**

**第一步：!important 最高优先级**
```
!important 规则：
  .container .text { 18px !important; }  (0,2,0)
  .target { 14px !important; }           (0,1,0)
```

**第二步：比较 !important 规则的特异性**
```
(0,2,0) vs (0,1,0)
18px 胜出 ✅
```

**最终：font-size: 18px**

**完整排序：**
```
1. .container .text { 18px !important; }  (0,2,0) ← 最高
2. .target { 14px !important; }           (0,1,0)
3. #app .target { 16px; }                 (1,1,0)
4. div div p { 20px; }                    (0,0,3)
```

**关键规则：**
- !important 声明优先于普通声明
- !important 之间仍比较特异性
- 特异性高的 !important 获胜

</details>

---

## 第 9 题 🔴

**类型：** 代码题  
**标签：** @layer 优先级

### 题目

以下代码中，`.box` 的颜色是？

```css
@layer A, B;

.box { color: green; }

@layer B {
  .box { color: blue; }
}

@layer A {
  #box { color: red; }
}
```

```html
<div id="box" class="box">Text</div>
```

**选项：**
- A. red
- B. blue
- C. green
- D. 无颜色

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**@layer 优先级规则**

**核心规则：无层样式 > 有层样式**

**优先级顺序：**
```
1. 无层样式
   .box { color: green; }  ← 最高优先级 ✅

2. @layer B
   .box { color: blue; }

3. @layer A
   #box { color: red; }  ← 即使ID选择器也无效
```

**详细分析：**

**层的声明顺序：**
```css
@layer A, B;  /* A在前，B在后 */
```

**层内优先级：**
```
B层 > A层（后声明优先）
```

**但是：**
```
无层样式 > 任何层样式
```

**因此：**
```
.box { color: green; }      ← 胜出（无层）
  ↓
@layer B { .box { blue; } } ← 层样式（被覆盖）
  ↓
@layer A { #box { red; } }  ← 层样式（被覆盖）
```

**如果都在层内：**
```css
@layer A, B;

@layer A {
  .box { color: green; }
}

@layer B {
  #box { color: red; }  /* B层后声明 */
}

/* 最终：red（B层优先，即使特异性低）*/
```

**完整优先级：**
```
1. 无层 !important
2. 层内 !important（按层顺序）
3. 无层样式               ← green 在此
4. 层内样式（按层顺序）    ← red, blue 在此
```

**最佳实践：**
```css
/* 声明层顺序 */
@layer reset, base, components, utilities;

/* 关键样式放无层 */
.critical { color: red; }  /* 始终优先 */

/* 普通样式放层内 */
@layer components {
  .btn { padding: 10px; }
}
```

</details>

---

## 第 10 题 🔴

**类型：** 多选题  
**标签：** 层叠最佳实践

### 题目

关于 CSS 层叠的最佳实践，以下说法正确的是？

**选项：**
- A. 避免过度使用 !important
- B. 保持选择器特异性扁平化
- C. 使用 @layer 组织样式
- D. ID 选择器优于类选择器

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, C

### 📖 解析

**层叠最佳实践**

**✅ A. 避免 !important**
```css
/* ❌ 不好 */
.box { color: red !important; }
.box { background: blue !important; }

/* ✅ 好 */
.box { color: red; }
.box-special { color: blue; }
```

**原因：**
- 难以覆盖
- 破坏层叠规则
- 难以维护

**合理使用场景：**
```css
/* 工具类 */
.hidden { display: none !important; }

/* 覆盖第三方库 */
.override { color: red !important; }
```

**✅ B. 扁平化特异性**
```css
/* ❌ 深层嵌套 */
#header .nav .menu .item a { }

/* ✅ 扁平化 */
.nav-link { }
```

**好处：**
- 易于覆盖
- 性能更好
- 可维护性高

**✅ C. 使用 @layer**
```css
@layer reset, base, components, utilities;

@layer reset {
  * { margin: 0; }
}

@layer components {
  .btn { padding: 10px; }
}
```

**好处：**
- 明确层叠顺序
- 易于管理
- 避免特异性战争

**❌ D. ID vs 类（错误）**
```css
/* ❌ ID选择器 */
#header { }  /* 特异性太高，难覆盖 */

/* ✅ 类选择器 */
.header { }  /* 灵活，易维护 */
```

**其他最佳实践：**

**1. BEM命名**
```css
.block { }
.block__element { }
.block--modifier { }
```

**2. 组合而非嵌套**
```css
/* ❌ */
.header .nav li a { }

/* ✅ */
.header-nav-link { }
```

**3. 单一职责**
```css
/* 样式分离 */
.btn { }        /* 结构 */
.btn-primary { }/* 皮肤 */
.btn-large { }  /* 大小 */
```

**4. 渐进增强**
```css
.box {
  width: 100%;
}

@supports (display: grid) {
  .box { width: auto; }
}
```

</details>

---

**导航**  
[上一章：第 6 章 - 样式表加载与阻塞](./chapter-06.md) | [返回目录](../README.md) | [下一章：第 8 章 - 继承机制](./chapter-08.md)
