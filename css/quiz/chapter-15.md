# 第 15 章：IFC 行内格式化上下文 - 面试题

**难度分布：** 🟢 简单 x3 | 🟡 中等 x4 | 🔴 困难 x3

---

## 第 1 题 🟢

**类型：** 单选题  
**标签：** IFC 定义

### 题目

IFC（Inline Formatting Context）是什么？

**选项：**
- A. 行内格式化上下文
- B. 图片格式化上下文
- C. 内联框架上下文
- D. 行内浮动上下文

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A

### 📖 解析

**IFC - Inline Formatting Context（行内格式化上下文）**

**定义：**
- 行内元素的布局环境
- 元素水平排列
- 形成行盒（line box）

**触发条件：**
```css
/* 包含行内元素的块级盒会创建 IFC */
<div>
  <span>inline 1</span>
  <span>inline 2</span>
</div>
```

</details>

---

## 第 2 题 🟢

**类型：** 单选题  
**标签：** 行盒

### 题目

行盒（line box）的高度由什么决定？

**选项：**
- A. 父元素的 height
- B. 内部最高的行内盒
- C. line-height 属性
- D. font-size 属性

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**行盒高度计算**

```html
<div>
  <span style="font-size: 16px;">Small</span>
  <span style="font-size: 32px;">Large</span>
</div>
```

**高度：**
- 由内部最高的行内盒决定
- 考虑 line-height
- 受 vertical-align 影响

**示例：**
```css
.container { line-height: 1.5; }

span {
  font-size: 16px;
  /* 行内盒高度 = 16px × 1.5 = 24px */
}

/* 行盒高度 = 最高行内盒的高度 */
```

</details>

---

## 第 3 题 🟢

**类型：** 判断题  
**标签：** vertical-align

### 题目

`vertical-align` 属性用于控制行内元素的垂直对齐方式。

**选项：**
- A. ✅ 正确
- B. ❌ 错误

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A（正确）

### 📖 解析

**vertical-align 作用**

```css
img {
  vertical-align: middle;  /* 垂直居中 */
}
```

**常用值：**
```css
vertical-align: baseline;    /* 基线对齐（默认）*/
vertical-align: top;         /* 顶部对齐 */
vertical-align: middle;      /* 中线对齐 */
vertical-align: bottom;      /* 底部对齐 */
vertical-align: text-top;    /* 文本顶部 */
vertical-align: text-bottom; /* 文本底部 */
vertical-align: 10px;        /* 相对基线上移 */
vertical-align: -5px;        /* 相对基线下移 */
```

**注意：**
- 只对行内元素和 table-cell 有效
- 对块级元素无效

</details>

---

## 第 4 题 🟡

**类型：** 代码题  
**标签：** 图片底部间隙

### 题目

以下代码中，图片底部为什么会有间隙？

```html
<div>
  <img src="image.jpg">
</div>
```

```css
div {
  background: lightblue;
}

img {
  display: inline;  /* 或默认 */
}
```

**选项：**
- A. margin 默认值
- B. padding 默认值
- C. 图片基线对齐，基线下方有空间
- D. CSS 错误

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**图片底部间隙问题**

**原因：**
```css
img {
  vertical-align: baseline;  /* 默认值 */
}
/* 图片按基线对齐，基线下方预留了字母下沉部分的空间 */
```

**解决方案：**

**方案1：vertical-align**
```css
img {
  vertical-align: bottom;  /* 或 middle/top */
}
```

**方案2：display**
```css
img {
  display: block;
}
```

**方案3：line-height**
```css
div {
  line-height: 0;
}
```

**方案4：font-size**
```css
div {
  font-size: 0;
}
```

**可视化：**
```
┌──────────┐
│   ┌────┐ │
│   │img │ │
│   └────┘ │
│    ↑     │ ← 基线
│   间隙   │ ← 字母下沉空间（g, y, p）
└──────────┘
```

</details>

---

## 第 5 题 🟡

**类型：** 多选题  
**标签：** line-height

### 题目

关于 `line-height`，以下说法正确的是？

**选项：**
- A. 无单位数值会被子元素继承
- B. 百分比会被转换为像素后继承
- C. 可以为负值
- D. 影响行盒的高度

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, D

### 📖 解析

**line-height 特性**

**✅ A. 无单位数值继承**
```css
.parent {
  font-size: 16px;
  line-height: 1.5;  /* 无单位 */
}

.child {
  font-size: 20px;
  /* 继承 1.5，计算为 20px × 1.5 = 30px */
}
```

**✅ B. 百分比转换**
```css
.parent {
  font-size: 16px;
  line-height: 150%;  /* 计算为 24px */
}

.child {
  font-size: 20px;
  /* 继承 24px（固定值）*/
}
```

**❌ C. 不能为负值**
```css
line-height: -1;  /* 无效 */
```

**✅ D. 影响行盒高度**
```css
.text {
  line-height: 2;
  /* 行盒高度 = font-size × 2 */
}
```

**推荐用法：**
```css
/* ✅ 推荐：无单位数值 */
body {
  line-height: 1.6;
}

/* ⚠️ 避免：固定像素 */
body {
  line-height: 24px;  /* 子元素无法自适应 */
}
```

</details>

---

## 第 6 题 🟡

**类型：** 代码题  
**标签：** 文本居中

### 题目

如何实现单行文本垂直居中？

**选项：**
- A. `line-height` 等于容器高度
- B. `vertical-align: middle`
- C. `padding` 上下相等
- D. 以上都可以

<details>
<summary>查看答案</summary>

### ✅ 正确答案：D

### 📖 解析

**单行文本垂直居中方法**

**✅ A. line-height（最常用）**
```css
.box {
  height: 100px;
  line-height: 100px;
}
```

**✅ B. vertical-align（配合 table-cell）**
```css
.box {
  display: table-cell;
  height: 100px;
  vertical-align: middle;
}
```

**✅ C. padding**
```css
.box {
  padding-top: 40px;
  padding-bottom: 40px;
}
```

**其他方法：**

**Flexbox**
```css
.box {
  display: flex;
  align-items: center;
  height: 100px;
}
```

**Grid**
```css
.box {
  display: grid;
  place-items: center;
  height: 100px;
}
```

**最佳实践：**
```css
/* 单行文本：line-height */
.single-line {
  height: 50px;
  line-height: 50px;
}

/* 多行文本：flexbox */
.multi-line {
  display: flex;
  align-items: center;
  min-height: 50px;
}
```

</details>

---

## 第 7 题 🟡

**类型：** 单选题  
**标签：** white-space

### 题目

`white-space: nowrap` 的作用是？

**选项：**
- A. 保留空白符
- B. 不换行，超出部分隐藏
- C. 不换行，超出部分显示
- D. 自动换行

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**white-space 属性**

```css
.text {
  white-space: nowrap;
}
/* 文本不换行，超出显示（需配合 overflow 处理）*/
```

**所有值：**

**nowrap**
```css
white-space: nowrap;
/* 不换行，超出显示 */
```

**normal**
```css
white-space: normal;
/* 默认，合并空白，自动换行 */
```

**pre**
```css
white-space: pre;
/* 保留空白，不换行 */
```

**pre-wrap**
```css
white-space: pre-wrap;
/* 保留空白，自动换行 */
```

**pre-line**
```css
white-space: pre-line;
/* 合并空白，保留换行 */
```

**对比表：**

| 值 | 合并空白 | 换行符 | 自动换行 |
|----|---------|--------|---------|
| normal | ✅ | ❌ | ✅ |
| nowrap | ✅ | ❌ | ❌ |
| pre | ❌ | ✅ | ❌ |
| pre-wrap | ❌ | ✅ | ✅ |
| pre-line | ✅ | ✅ | ✅ |

**实用组合：**
```css
/* 单行省略 */
.ellipsis {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
```

</details>

---

## 第 8 题 🔴

**类型：** 代码题  
**标签：** vertical-align 计算

### 题目

以下代码中，span 元素相对基线上移多少？

```css
div {
  font-size: 16px;
  line-height: 1.5;
}

span {
  font-size: 20px;
  vertical-align: 50%;
}
```

**选项：**
- A. 8px
- B. 10px
- C. 12px
- D. 15px

<details>
<summary>查看答案</summary>

### ✅ 正确答案：D

### 📖 解析

**vertical-align 百分比计算**

**规则：百分比相对于元素自身的 line-height**

```css
span {
  font-size: 20px;
  line-height: 1.5;  /* 继承父元素 */
  vertical-align: 50%;
}
```

**计算：**
```
1. span 的 line-height = 20px × 1.5 = 30px
2. vertical-align: 50% = 30px × 50% = 15px
3. 结果：上移 15px ✅
```

**其他单位：**
```css
/* 固定值 */
vertical-align: 10px;    /* 上移 10px */
vertical-align: -5px;    /* 下移 5px */

/* 百分比 */
vertical-align: 50%;     /* 相对自身 line-height */

/* 关键字 */
vertical-align: middle;  /* 中线对齐 */
```

**注意：**
- 百分比相对自身 line-height
- 不是相对父元素

</details>

---

## 第 9 题 🔴

**类型：** 代码题  
**标签：** 行盒复杂场景

### 题目

以下代码会生成几个行盒？

```html
<div style="width: 100px;">
  <span>Short</span>
  <span>Very long text that will definitely wrap to next line</span>
</div>
```

**选项：**
- A. 1个
- B. 2个
- C. 3个
- D. 由内容决定

<details>
<summary>查看答案</summary>

### ✅ 正确答案：D

### 📖 解析

**行盒生成规则**

**自动换行：**
```
容器宽度：100px
文本内容超出后自动换行
每一行形成一个行盒
```

**可能的布局：**
```
行盒1: [Short] [Very]
行盒2: [long text]
行盒3: [that will]
行盒4: [definitely]
行盒5: [wrap to next]
行盒6: [line]
```

**具体行盒数量取决于：**
- 容器宽度
- 文本内容长度
- 字体大小
- 字符宽度

**换行规则：**
```css
/* 默认换行 */
word-wrap: normal;

/* 强制换行 */
word-wrap: break-word;
word-break: break-all;

/* 不换行 */
white-space: nowrap;
```

**因此答案是 D：由内容决定**

</details>

---

## 第 10 题 🔴

**类型：** 多选题  
**标签：** IFC 特性

### 题目

关于 IFC，以下说法正确的是？

**选项：**
- A. 行内盒水平排列
- B. 行内盒不能设置宽高
- C. vertical-align 控制垂直对齐
- D. 行盒高度由 line-height 决定

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, C

### 📖 解析

**IFC 特性分析**

**✅ A. 水平排列**
```html
<div>
  <span>A</span><span>B</span><span>C</span>
</div>
<!-- 水平排列：A B C -->
```

**❌ B. 宽高设置（部分错误）**
```css
/* inline 不能设置宽高 */
span {
  display: inline;
  width: 100px;  /* 无效 */
}

/* inline-block 可以设置 */
span {
  display: inline-block;
  width: 100px;  /* 有效 ✅ */
}
```

**✅ C. vertical-align**
```css
img {
  vertical-align: middle;
}
```

**❌ D. line-height（部分正确）**
```
行盒高度由多个因素决定：
1. 内部最高的行内盒
2. line-height
3. vertical-align
4. font-size
```

**完整理解：**

**IFC 特点：**
- 行内盒水平排列
- 宽度不够时自动换行
- 形成行盒（line box）
- 垂直对齐由 vertical-align 控制

**行盒高度：**
```
由内部所有行内盒的高度和对齐方式共同决定
不仅仅是 line-height
```

</details>

---

**导航**  
[上一章：第 14 章 - BFC](./chapter-14.md) | [返回目录](../README.md) | [下一章：第 16 章 - FFC](./chapter-16.md)
