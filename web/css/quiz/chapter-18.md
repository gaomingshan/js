# 第 18 章：层叠上下文原理 - 面试题

**难度分布：** 🟢 简单 x3 | 🟡 中等 x4 | 🔴 困难 x3

---

## 第 1 题 🟢

**类型：** 单选题  
**标签：** 层叠上下文定义

### 题目

层叠上下文（Stacking Context）是什么？

**选项：**
- A. CSS 选择器的优先级
- B. 元素在 Z 轴上的三维空间
- C. 元素的堆叠顺序
- D. 页面的层叠顺序

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**层叠上下文（Stacking Context）**

**定义：**
- 元素在 Z 轴（垂直于屏幕）上的三维空间
- 决定元素的层叠顺序
- 独立的渲染层

**可视化：**
```
屏幕
  ↓
Z轴 ⊙ → 层叠上下文
  ↓
背景
```

**作用：**
- 控制元素的前后关系
- 影响 `z-index` 的作用域
- 创建独立的层叠环境

</details>

---

## 第 2 题 🟢

**类型：** 多选题  
**标签：** 创建条件

### 题目

以下哪些情况会创建层叠上下文？

**选项：**
- A. 根元素（html）
- B. `position: relative` 且 `z-index` 不为 auto
- C. `opacity` 小于 1
- D. `transform` 不为 none

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, C, D

### 📖 解析

**创建层叠上下文的条件（全部正确）**

**✅ A. 根元素**
```html
<html>  <!-- 创建根层叠上下文 -->
```

**✅ B. position + z-index**
```css
.box {
  position: relative;  /* 或 absolute、fixed */
  z-index: 1;  /* 不为 auto */
}
```

**✅ C. opacity**
```css
.box {
  opacity: 0.99;  /* 小于 1 */
}
```

**✅ D. transform**
```css
.box {
  transform: translateZ(0);  /* 不为 none */
}
```

**完整列表：**
```css
/* 定位 + z-index */
position: relative/absolute/fixed + z-index ≠ auto

/* 透明度 */
opacity < 1

/* 变换 */
transform ≠ none
transform-style: preserve-3d

/* 滤镜 */
filter ≠ none

/* 其他 */
perspective ≠ none
mix-blend-mode ≠ normal
isolation: isolate
will-change: opacity/transform

/* 布局 */
display: flex/grid 的子元素 + z-index ≠ auto
contain: layout/paint
```

</details>

---

## 第 3 题 🟢

**类型：** 判断题  
**标签：** z-index 作用域

### 题目

不同层叠上下文中的元素，`z-index` 值可以直接比较。

**选项：**
- A. ✅ 正确
- B. ❌ 错误

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B（错误）

### 📖 解析

**z-index 的作用域限制**

**规则：z-index 只在同一层叠上下文内比较**

```html
<div style="position: relative; z-index: 1;">
  <div style="position: relative; z-index: 9999;">Child A</div>
</div>

<div style="position: relative; z-index: 2;">
  <div style="position: relative; z-index: 1;">Child B</div>
</div>
```

**结果：**
- Child B 在 Child A 之上
- 因为父元素 z-index: 2 > 1
- 子元素的 z-index 在父级层叠上下文内比较

**层叠顺序：**
```
父 z:2
  └─ 子 z:1  ← 显示在上层
父 z:1
  └─ 子 z:9999  ← 显示在下层
```

</details>

---

## 第 4 题 🟡

**类型：** 代码题  
**标签：** 层叠顺序

### 题目

以下元素的层叠顺序从下到上是？

```css
1. 背景/边框
2. 负 z-index
3. 块级盒
4. 浮动盒
5. 行内盒
6. z-index: 0
7. 正 z-index
```

**选项：**
- A. 1-2-3-4-5-6-7
- B. 1-3-4-5-2-6-7
- C. 2-1-3-4-5-6-7
- D. 1-2-4-3-5-6-7

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A

### 📖 解析

**层叠顺序（从下到上）**

```
7. 正 z-index               ⬆️ 最上层
6. z-index: 0/auto
5. 行内盒
4. 浮动盒
3. 块级盒
2. 负 z-index
1. 背景/边框                ⬇️ 最下层
```

**详细说明：**

**层级1：背景和边框**
```css
.box {
  background: red;
  border: 1px solid black;
}
/* 最底层 */
```

**层级2：负 z-index**
```css
.box {
  position: relative;
  z-index: -1;
}
/* 在背景之上，内容之下 */
```

**层级3-5：正常流**
```
块级盒 < 浮动盒 < 行内盒
```

**层级6：z-index: 0**
```css
.box {
  position: relative;
  z-index: 0;
}
```

**层级7：正 z-index**
```css
.box {
  position: relative;
  z-index: 1;
}
/* 最上层 */
```

**可视化：**
```
┌─────────────────────┐
│ z-index: 1          │ ← 层7
├─────────────────────┤
│ z-index: 0          │ ← 层6
├─────────────────────┤
│ 行内盒               │ ← 层5
├─────────────────────┤
│ 浮动盒               │ ← 层4
├─────────────────────┤
│ 块级盒               │ ← 层3
├─────────────────────┤
│ z-index: -1         │ ← 层2
├─────────────────────┤
│ 背景/边框            │ ← 层1
└─────────────────────┘
```

</details>

---

## 第 5 题 🟡

**类型：** 代码题  
**标签：** opacity 层叠

### 题目

以下代码中，`.child` 能否通过 z-index 显示在 `.sibling` 之上？

```html
<div class="parent">
  <div class="child">Child</div>
</div>
<div class="sibling">Sibling</div>
```

```css
.parent {
  opacity: 0.99;
}

.child {
  position: relative;
  z-index: 9999;
}

.sibling {
  position: relative;
  z-index: 1;
}
```

**选项：**
- A. 可以
- B. 不可以
- C. 取决于浏览器
- D. 需要更多信息

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**opacity 创建层叠上下文**

```css
.parent {
  opacity: 0.99;  /* 创建层叠上下文 */
}
```

**层叠结构：**
```
根层叠上下文
  ├─ parent（opacity层叠上下文）
  │   └─ child (z:9999)  ← 限制在parent内
  └─ sibling (z:1)
```

**比较顺序：**
1. parent 和 sibling 在同一层叠上下文
2. parent 没有 z-index，默认为 auto
3. sibling 有 z-index: 1
4. **sibling 显示在上层**

**child 的 z-index: 9999 无效，因为：**
- child 在 parent 创建的层叠上下文内
- 无法突破父级层叠上下文
- 只能与 parent 内的兄弟元素比较

**解决方案：**
```css
.parent {
  opacity: 0.99;
  position: relative;
  z-index: 2;  /* 使 parent 在 sibling 之上 */
}
```

</details>

---

## 第 6 题 🟡

**类型：** 单选题  
**标签：** transform 层叠

### 题目

`transform: translateZ(0)` 常用于什么目的？

**选项：**
- A. 3D 变换
- B. 创建层叠上下文，开启硬件加速
- C. 改变元素位置
- D. 提高 z-index

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**transform: translateZ(0) 的作用**

```css
.box {
  transform: translateZ(0);
}
```

**主要目的：**
1. 创建层叠上下文
2. 开启 GPU 硬件加速
3. 提升动画性能

**性能优化：**
```css
/* 提升到合成层 */
.animated {
  transform: translateZ(0);
  /* 或 */
  will-change: transform;
}
```

**对比：**
```css
/* 不创建层叠上下文 */
.box {
  transform: none;
}

/* 创建层叠上下文 */
.box {
  transform: translateZ(0);     /* 3D变换 */
  transform: translate(0, 0);   /* 2D变换也可以 */
}
```

**实际应用：**
```css
/* 修复层叠问题 */
.modal {
  transform: translateZ(0);
  z-index: 1000;
}

/* 性能优化 */
.slide {
  transform: translateZ(0);
  transition: transform 0.3s;
}
```

</details>

---

## 第 7 题 🟡

**类型：** 代码题  
**标签：** flex/grid 子元素

### 题目

以下代码中，`.item` 是否创建层叠上下文？

```css
.container {
  display: flex;
}

.item {
  z-index: 1;
}
```

**选项：**
- A. 创建
- B. 不创建
- C. 取决于浏览器
- D. 需要 position

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A

### 📖 解析

**flex/grid 子元素的特殊性**

**规则：flex/grid 子元素 + z-index ≠ auto 会创建层叠上下文**

```css
.container {
  display: flex;  /* 或 grid */
}

.item {
  z-index: 1;  /* 创建层叠上下文 ✅ */
  /* 不需要 position! */
}
```

**对比普通元素：**
```css
/* 普通块级元素 */
.normal {
  z-index: 1;  /* 无效，不创建层叠上下文 ❌ */
}

/* 需要定位 */
.positioned {
  position: relative;
  z-index: 1;  /* 创建层叠上下文 ✅ */
}
```

**完整规则：**

**创建层叠上下文：**
```css
/* 普通元素 */
position: relative/absolute/fixed + z-index ≠ auto

/* flex/grid 子元素 */
display: flex/grid 的子元素 + z-index ≠ auto
/* 不需要 position */
```

**示例：**
```css
.flex-container {
  display: flex;
}

.item1 {
  z-index: 1;  /* 有效 ✅ */
}

.item2 {
  z-index: 2;  /* 有效 ✅ */
}
```

</details>

---

## 第 8 题 🔴

**类型：** 代码题  
**标签：** 复杂层叠

### 题目

以下元素的显示顺序从下到上是？

```html
<div class="a">A</div>
<div class="b">
  <div class="b1">B1</div>
</div>
<div class="c">C</div>
```

```css
.a {
  position: relative;
  z-index: 1;
}

.b {
  position: relative;
  z-index: 2;
}

.b1 {
  position: relative;
  z-index: -1;
}

.c {
  position: relative;
  z-index: 3;
}
```

**选项：**
- A. A, B1, B, C
- B. B1, A, B, C
- C. A, B, B1, C
- D. A, B (B1在B下), C

<details>
<summary>查看答案</summary>

### ✅ 正确答案：D

### 📖 解析

**层叠上下文嵌套规则**

**层叠结构：**
```
根层叠上下文
  ├─ A (z:1)
  ├─ B (z:2)
  │   └─ B1 (z:-1)  ← 在 B 的层叠上下文内
  └─ C (z:3)
```

**比较规则：**

**1. 同级元素比较 z-index**
```
C (z:3) > B (z:2) > A (z:1)
```

**2. B1 在 B 内部**
```
B1 (z:-1) 是负值
在 B 的背景之上，B 的内容之下
无法突破 B 的层叠上下文
```

**最终顺序（从下到上）：**
```
A (z:1)
  ↓
B (z:2)
  └─ B1 (z:-1，在 B 背景之上但在 B 内容之下)
  ↓
C (z:3)
```

**可视化：**
```
┌─────┐
│  C  │ z:3  ← 最上层
└─────┘

┌─────┐
│  B  │ z:2
│ ┌──┐│
│ │B1││ z:-1 (相对 B)
│ └──┘│
└─────┘

┌─────┐
│  A  │ z:1  ← 最下层
└─────┘
```

</details>

---

## 第 9 题 🔴

**类型：** 代码题  
**标签：** isolation

### 题目

`isolation: isolate` 的作用是什么？

**选项：**
- A. 隔离 margin
- B. 创建层叠上下文
- C. 隔离 z-index
- D. 防止事件冒泡

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**isolation: isolate**

```css
.box {
  isolation: isolate;  /* 创建层叠上下文 */
}
```

**作用：**
- 创建新的层叠上下文
- 隔离混合模式效果
- 不影响其他属性

**使用场景：**

**1. 隔离 mix-blend-mode**
```css
.parent {
  isolation: isolate;  /* 限制混合范围 */
}

.child {
  mix-blend-mode: multiply;
  /* 只与 parent 内的元素混合 */
}
```

**2. 控制层叠**
```css
.modal-container {
  isolation: isolate;
  /* 创建独立的层叠环境 */
}
```

**3. 避免层叠问题**
```css
.component {
  isolation: isolate;
  /* 子元素的 z-index 不影响外部 */
}
```

**对比其他创建方式：**
```css
/* 创建层叠上下文，但有副作用 */
opacity: 0.99;        /* 改变透明度 */
transform: translateZ(0);  /* 开启3D */
position: relative;
z-index: 0;

/* 纯粹创建层叠上下文，无副作用 */
isolation: isolate;   /* ✅ 推荐 */
```

</details>

---

## 第 10 题 🔴

**类型：** 多选题  
**标签：** 综合应用

### 题目

关于层叠上下文，以下说法正确的是？

**选项：**
- A. 层叠上下文可以嵌套
- B. 子元素的 z-index 只在父级层叠上下文内有效
- C. opacity < 1 会创建层叠上下文
- D. 所有元素都在根层叠上下文中

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, C

### 📖 解析

**层叠上下文综合分析**

**✅ A. 可以嵌套**
```
根层叠上下文
  ├─ 层叠上下文 A
  │   └─ 层叠上下文 A1
  └─ 层叠上下文 B
```

**✅ B. z-index 作用域**
```css
.parent {
  opacity: 0.99;  /* 创建层叠上下文 */
}

.child {
  z-index: 9999;
  /* 只在 parent 内部有效 */
}
```

**✅ C. opacity 创建**
```css
.box {
  opacity: 0.99;  /* < 1 创建层叠上下文 */
}
```

**❌ D. 不是所有元素都在根层叠上下文**
```
元素可能在：
- 根层叠上下文
- 父元素创建的层叠上下文
- 自己创建的层叠上下文
```

**完整理解：**

**层叠上下文树：**
```html
<html>                    <!-- 根 -->
  <div style="opacity: 0.99;">  <!-- 层叠上下文 A -->
    <div style="z-index: 1;"></div>  <!-- 在 A 内 -->
  </div>
  <div style="z-index: 2;"></div>    <!-- 在根内 -->
</html>
```

**调试技巧：**
```javascript
// 检查元素是否创建层叠上下文
getComputedStyle(element).zIndex !== 'auto'
getComputedStyle(element).opacity < 1
getComputedStyle(element).transform !== 'none'
```

</details>

---

**导航**  
[上一章：第 17 章 - GFC](./chapter-17.md) | [返回目录](../README.md) | [下一章：第 19 章 - z-index详解](./chapter-19.md)
