# 第 19 章：z-index 详解 - 面试题

**难度分布：** 🟢 简单 x3 | 🟡 中等 x4 | 🔴 困难 x3

---

## 第 1 题 🟢

**类型：** 单选题  
**标签：** z-index 基础

### 题目

`z-index` 属性对哪种元素有效？

**选项：**
- A. 所有元素
- B. 只对定位元素有效
- C. 只对块级元素有效
- D. 只对行内元素有效

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**z-index 的应用条件**

```css
/* ✅ 有效：定位元素 */
.box {
  position: relative;  /* 或 absolute、fixed、sticky */
  z-index: 10;
}

/* ❌ 无效：静态定位 */
.static {
  position: static;  /* 默认值 */
  z-index: 10;  /* 无效 */
}
```

**特殊情况：flex/grid 子元素**
```css
.flex-container {
  display: flex;
}

.flex-item {
  /* 不需要 position */
  z-index: 1;  /* 有效 ✅ */
}
```

**总结：**
- static 定位：z-index 无效
- relative/absolute/fixed/sticky：z-index 有效
- flex/grid 子元素：z-index 有效（无需 position）

</details>

---

## 第 2 题 🟢

**类型：** 单选题  
**标签：** z-index 默认值

### 题目

`z-index` 的默认值是？

**选项：**
- A. 0
- B. 1
- C. auto
- D. none

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**z-index 默认值：auto**

```css
.box {
  position: relative;
  /* z-index: auto;  默认值 */
}
```

**auto vs 0 的区别：**

**z-index: auto**
```css
.box {
  z-index: auto;
  /* 不创建层叠上下文 */
  /* 层叠顺序由父元素决定 */
}
```

**z-index: 0**
```css
.box {
  position: relative;
  z-index: 0;
  /* 创建层叠上下文 ✅ */
  /* 层叠顺序为 0 */
}
```

**关键区别：**
```
auto：不创建层叠上下文
0：创建层叠上下文

但在同一层级中，auto 和 0 的层叠顺序相同
```

</details>

---

## 第 3 题 🟢

**类型：** 判断题  
**标签：** z-index 负值

### 题目

`z-index` 可以设置为负值。

**选项：**
- A. ✅ 正确
- B. ❌ 错误

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A（正确）

### 📖 解析

**z-index 可以为负值**

```css
.box {
  position: relative;
  z-index: -1;  /* 有效 ✅ */
}
```

**负值的效果：**
- 在父元素的背景和边框之上
- 在父元素的内容之下

**示例：**
```html
<div class="parent">
  Parent Content
  <div class="negative">Negative z-index</div>
</div>
```

```css
.parent {
  position: relative;
  background: lightblue;
  padding: 20px;
}

.negative {
  position: absolute;
  z-index: -1;
  /* 在背景之上，文字之下 */
}
```

**层叠顺序：**
```
正 z-index     ⬆️
z-index: 0
内容
负 z-index
背景/边框      ⬇️
```

</details>

---

## 第 4 题 🟡

**类型：** 代码题  
**标签：** z-index 比较

### 题目

以下元素的层叠顺序从下到上是？

```css
.a { position: relative; z-index: 10; }
.b { position: relative; z-index: 5; }
.c { position: relative; z-index: 15; }
.d { position: relative; }  /* 无 z-index */
```

**选项：**
- A. d, b, a, c
- B. b, a, c, d
- C. d, b, a, c (d 在最下)
- D. b, d, a, c

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A

### 📖 解析

**z-index 排序规则**

**值比较：**
```
c (z:15) > a (z:10) > b (z:5) > d (auto)
```

**层叠顺序（从下到上）：**
```
d (z:auto)  ← 最下层
b (z:5)
a (z:10)
c (z:15)    ← 最上层
```

**关键点：**
- z-index: auto 等同于 z-index: 0 的层叠顺序
- 但 auto 不创建层叠上下文
- 在同级比较中，auto 在所有正值之下

**详细层叠层级：**
```
层7: z-index: 15 (c)
层6: z-index: 10 (a)
层5: z-index: 5 (b)
层4: z-index: auto (d)
```

**如果都是 auto：**
```css
.all-auto { position: relative; }
/* 按 DOM 顺序，后面的在上面 */
```

</details>

---

## 第 5 题 🟡

**类型：** 多选题  
**标签：** z-index 失效

### 题目

以下哪些情况会导致 `z-index` 看起来"失效"？

**选项：**
- A. 元素是 `position: static`
- B. 父元素创建了层叠上下文
- C. 元素没有设置宽高
- D. 元素是 flex/grid 容器（非子元素）

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B

### 📖 解析

**z-index 失效原因**

**✅ A. position: static**
```css
.box {
  position: static;  /* 默认值 */
  z-index: 999;  /* 无效 ❌ */
}
```

**✅ B. 父元素层叠上下文**
```css
.parent {
  opacity: 0.99;  /* 创建层叠上下文 */
}

.child {
  position: relative;
  z-index: 9999;
  /* 无法超越 parent 的层级 */
}
```

**❌ C. 宽高无关（错误）**
```css
.box {
  position: relative;
  z-index: 10;
  width: 0;
  height: 0;
  /* z-index 仍然有效 ✅ */
}
```

**❌ D. flex/grid 容器（错误）**
```css
/* flex 容器本身 */
.flex-container {
  display: flex;
  position: relative;
  z-index: 10;  /* 有效 ✅ */
}

/* flex 子元素 */
.flex-item {
  z-index: 1;  /* 有效 ✅（特殊情况）*/
}
```

**常见失效场景：**

**场景1：忘记定位**
```css
/* ❌ 错误 */
.box {
  z-index: 10;
}

/* ✅ 正确 */
.box {
  position: relative;
  z-index: 10;
}
```

**场景2：层叠上下文限制**
```css
.modal-container {
  transform: translateZ(0);  /* 创建层叠上下文 */
}

.modal {
  position: fixed;
  z-index: 9999;
  /* 被限制在 modal-container 内 */
}
```

</details>

---

## 第 6 题 🟡

**类型：** 代码题  
**标签：** 层叠上下文嵌套

### 题目

以下代码中，`.item2` 能否显示在 `.item1` 之上？

```html
<div class="parent1">
  <div class="item1">Item 1</div>
</div>
<div class="parent2">
  <div class="item2">Item 2</div>
</div>
```

```css
.parent1 {
  position: relative;
  z-index: 100;
}

.item1 {
  position: relative;
  z-index: 1;
}

.parent2 {
  position: relative;
  z-index: 10;
}

.item2 {
  position: relative;
  z-index: 9999;
}
```

**选项：**
- A. 可以
- B. 不可以
- C. 取决于 DOM 顺序
- D. 需要更多信息

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**层叠上下文的限制**

**层叠结构：**
```
根层叠上下文
  ├─ parent1 (z:100)
  │   └─ item1 (z:1)
  └─ parent2 (z:10)
      └─ item2 (z:9999)
```

**比较规则：**
1. 先比较 parent1 和 parent2
2. parent1 (z:100) > parent2 (z:10)
3. parent1 整体在上层

**结果：**
- item1 显示在 item2 之上
- item2 的 z:9999 无法突破 parent2
- 父元素的层级决定了子元素的上限

**可视化：**
```
┌──────────────┐
│ parent1      │ z:100  ← 整体在上
│  ┌────────┐  │
│  │ item1  │  │ z:1
│  └────────┘  │
└──────────────┘

┌──────────────┐
│ parent2      │ z:10   ← 整体在下
│  ┌────────┐  │
│  │ item2  │  │ z:9999 (无效)
│  └────────┘  │
└──────────────┘
```

**解决方案：**
```css
.parent2 {
  z-index: 101;  /* > parent1 */
}
```

</details>

---

## 第 7 题 🟡

**类型：** 单选题  
**标签：** z-index 最大值

### 题目

`z-index` 的最大有效值是？

**选项：**
- A. 999
- B. 9999
- C. 2147483647
- D. 无限大

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**z-index 取值范围**

```css
.box {
  z-index: 2147483647;  /* 最大值（2^31 - 1）*/
}
```

**范围：**
- 最小值：-2147483648 (-2^31)
- 最大值：2147483647 (2^31 - 1)
- 类型：32位有符号整数

**超出范围：**
```css
.box {
  z-index: 9999999999;  /* 超出范围 */
  /* 被截断为最大值 2147483647 */
}
```

**最佳实践：**
```css
/* ❌ 不推荐 */
.modal {
  z-index: 999999;  /* 过大 */
}

/* ✅ 推荐：使用合理的层级 */
.dropdown { z-index: 10; }
.modal { z-index: 100; }
.tooltip { z-index: 1000; }
.notification { z-index: 10000; }
```

**分层管理：**
```css
/* 定义层级常量 */
:root {
  --z-dropdown: 10;
  --z-sticky: 100;
  --z-modal: 1000;
  --z-toast: 10000;
}

.modal {
  z-index: var(--z-modal);
}
```

</details>

---

## 第 8 题 🔴

**类型：** 代码题  
**标签：** 复杂场景

### 题目

以下代码中，红色 div 能否显示在蓝色 div 之上？

```html
<div class="blue">
  <div class="red"></div>
</div>
```

```css
.blue {
  position: relative;
  z-index: 1;
  width: 200px;
  height: 200px;
  background: blue;
}

.red {
  position: absolute;
  z-index: -1;
  width: 100px;
  height: 100px;
  background: red;
  top: 50px;
  left: 50px;
}
```

**选项：**
- A. 可以
- B. 不可以
- C. 红色在蓝色背景之上，内容之下
- D. 完全不可见

<details>
<summary>查看答案</summary>

### ✅ 正确答案：C

### 📖 解析

**负 z-index 的特殊行为**

**层叠结构：**
```
.blue (z:1) 创建层叠上下文
  ├─ 背景/边框
  ├─ .red (z:-1) ← 在这里
  └─ 内容
```

**负 z-index 在父元素层叠上下文内的位置：**
- 在父元素的背景和边框之上
- 在父元素的内容之下

**可视化：**
```
┌─────────────────┐
│ .blue (蓝色背景) │
│  ┌──────────┐   │
│  │.red (红)  │   │ ← z:-1
│  └──────────┘   │
│  [内容在红色上方]│
└─────────────────┘
```

**如果 blue 有文字：**
```html
<div class="blue">
  Blue Text
  <div class="red"></div>
</div>
```

**层叠顺序：**
```
Blue Text (内容) ← 在上
.red (z:-1)      ← 在中
蓝色背景         ← 在下
```

**对比 z-index: 1：**
```css
.red {
  z-index: 1;  /* 在所有内容之上 */
}
```

</details>

---

## 第 9 题 🔴

**类型：** 代码题  
**标签：** transform 影响

### 题目

以下代码的层叠顺序是？

```html
<div class="a">A</div>
<div class="b">
  <div class="b-child">B Child</div>
</div>
```

```css
.a {
  position: relative;
  z-index: 1;
}

.b {
  transform: translateX(0);
}

.b-child {
  position: relative;
  z-index: 999;
}
```

**选项：**
- A. B Child 在 A 之上
- B. A 在 B Child 之上
- C. 两者层级相同
- D. B 在最上层

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**transform 创建层叠上下文**

```css
.b {
  transform: translateX(0);  /* 创建层叠上下文 */
}
```

**层叠结构：**
```
根层叠上下文
  ├─ A (z:1)
  └─ B (transform，无 z-index，默认 z:auto)
      └─ B-child (z:999，限制在 B 内)
```

**比较：**
1. A 和 B 在根层叠上下文中
2. A 有 z-index: 1
3. B 有 transform 但无 z-index（默认 auto）
4. 有 z-index 的元素 > auto

**结果：A 在上层**

**关键点：**
- transform 创建层叠上下文
- 但如果没有 z-index，层级为 auto（0级）
- B-child 的 z:999 无法突破 B 的层叠上下文

**解决方案：**
```css
.b {
  transform: translateX(0);
  position: relative;
  z-index: 2;  /* 使 B 在 A 之上 */
}
```

**完整层叠顺序：**
```
有 z-index (A, z:1)
  ↓
层叠上下文但 z:auto (B)
  └─ 子元素 z:999 (B-child)
```

</details>

---

## 第 10 题 🔴

**类型：** 多选题  
**标签：** 最佳实践

### 题目

关于 `z-index` 的最佳实践，以下说法正确的是？

**选项：**
- A. 使用变量管理 z-index 值
- B. 避免使用过大的值（如 999999）
- C. 建立清晰的层级体系
- D. 尽可能多用 z-index 控制层叠

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, C

### 📖 解析

**z-index 最佳实践**

**✅ A. 使用变量**
```css
:root {
  --z-dropdown: 10;
  --z-sticky: 100;
  --z-fixed: 1000;
  --z-modal: 10000;
  --z-toast: 100000;
}

.modal {
  z-index: var(--z-modal);
}
```

**✅ B. 避免过大值**
```css
/* ❌ 不推荐 */
.element {
  z-index: 9999999;
}

/* ✅ 推荐 */
.element {
  z-index: 100;
}
```

**✅ C. 建立层级体系**
```css
/* 层级规划 */
/* 1-9: 页面元素 */
.dropdown { z-index: 5; }

/* 10-99: 固定元素 */
.sticky-header { z-index: 50; }

/* 100-999: 悬浮元素 */
.tooltip { z-index: 500; }

/* 1000+: 模态框 */
.modal { z-index: 1000; }
.alert { z-index: 2000; }
```

**❌ D. 避免滥用（错误）**
```css
/* ❌ 过度使用 */
.element { z-index: 1; }
.another { z-index: 2; }
.third { z-index: 3; }

/* ✅ 合理使用 */
.element { }  /* 默认层级 */
.special { z-index: 10; }  /* 需要时才用 */
```

**完整最佳实践：**

**1. 分层管理**
```css
/* 定义层级常量 */
$z-indexes: (
  'base': 1,
  'dropdown': 10,
  'sticky': 100,
  'modal': 1000,
  'toast': 10000
);

.modal {
  z-index: map-get($z-indexes, 'modal');
}
```

**2. 文档化**
```css
/*
 * Z-Index 层级规划
 * 
 * 1-9: 基础层（页面元素）
 * 10-99: 交互层（下拉菜单）
 * 100-999: 固定层（sticky 元素）
 * 1000-9999: 遮罩层（模态框）
 * 10000+: 通知层（toast）
 */
```

**3. 避免层叠上下文陷阱**
```css
/* 注意这些属性会创建层叠上下文 */
.container {
  /* opacity, transform, filter 等 */
  /* 会限制子元素的 z-index */
}
```

**4. 调试技巧**
```javascript
// 查找所有有 z-index 的元素
$$('*').filter(el => {
  const z = getComputedStyle(el).zIndex;
  return z !== 'auto';
}).forEach(el => {
  console.log(el, getComputedStyle(el).zIndex);
});
```

</details>

---

**导航**  
[上一章：第 18 章 - 层叠上下文原理](./chapter-18.md) | [返回目录](../README.md) | [下一章：第 20 章 - 定位详解](./chapter-20.md)
