# 第 2 章：运算符与表达式 - 面试题

**难度分布：** 🟢 简单 x3 | 🟡 中等 x4 | 🔴 困难 x3

---

## 第 1 题 🟢

**类型：** 单选题  
**标签：** 运算符优先级

### 题目

以下代码的输出是什么？

```javascript
console.log(2 + 3 * 4);
```

**选项：**
- A. `20`
- B. `14`
- C. `11`
- D. `24`

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**运算符优先级规则**

```javascript
2 + 3 * 4
// 乘法优先级高于加法
= 2 + 12
= 14
```

**常见运算符优先级（从高到低）：**
1. **括号** `()`
2. **成员访问** `.` `[]`
3. **new**（带参数）
4. **函数调用** `()`
5. **new**（无参数）
6. **递增/递减** `++` `--`
7. **逻辑非/按位非** `!` `~`
8. **乘除模** `*` `/` `%`
9. **加减** `+` `-`
10. **比较** `<` `>` `<=` `>=`
11. **相等** `==` `!=` `===` `!==`
12. **逻辑与** `&&`
13. **逻辑或** `||`
14. **三元** `? :`
15. **赋值** `=` `+=` `-=`

**示例：**
```javascript
2 + 3 * 4           // 14
(2 + 3) * 4         // 20
2 + 3 * 4 - 1       // 13
2 + 3 * (4 - 1)     // 11
```

</details>

---

## 第 2 题 🟢

**类型：** 单选题  
**标签：** 递增递减运算符

### 题目

以下代码的输出是什么？

```javascript
let x = 5;
console.log(x++);
console.log(++x);
```

**选项：**
- A. `5`, `6`
- B. `5`, `7`
- C. `6`, `7`
- D. `6`, `6`

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**后置递增（x++）vs 前置递增（++x）**

**执行过程：**
```javascript
let x = 5;

// x++ 后置递增：先返回值，再递增
console.log(x++);  // 5（返回 5，然后 x 变成 6）

// ++x 前置递增：先递增，再返回值
console.log(++x);  // 7（x 先变成 7，然后返回 7）
```

**区别总结：**
- `x++`：返回原值，然后递增
- `++x`：先递增，返回新值

**更多示例：**
```javascript
let a = 10;
let b = a++;  // b = 10, a = 11
let c = ++a;  // c = 12, a = 12

console.log(a, b, c);  // 12, 10, 12
```

**在表达式中：**
```javascript
let x = 5;
let y = x++ + x++;
// 第一个 x++: 返回 5，x 变成 6
// 第二个 x++: 返回 6，x 变成 7
// y = 5 + 6 = 11
console.log(y, x);  // 11, 7

let a = 5;
let b = ++a + ++a;
// 第一个 ++a: a 变成 6，返回 6
// 第二个 ++a: a 变成 7，返回 7
// b = 6 + 7 = 13
console.log(b, a);  // 13, 7
```

</details>

---

## 第 3 题 🟢

**类型：** 判断题  
**标签：** 逻辑运算符

### 题目

逻辑与（`&&`）和逻辑或（`||`）运算符会将操作数转换为布尔值进行计算，但返回的是原始操作数的值。

**选项：**
- A. ✅ 正确
- B. ❌ 错误

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A（正确）

### 📖 解析

**逻辑运算符返回原始值，而非布尔值**

**逻辑与（&&）：**
```javascript
// 返回第一个假值，或最后一个真值
true && "hello"      // "hello"
"hello" && "world"   // "world"
false && "hello"     // false
null && "hello"      // null
"hello" && 0         // 0
```

**逻辑或（||）：**
```javascript
// 返回第一个真值，或最后一个假值
false || "hello"     // "hello"
"hello" || "world"   // "hello"
null || undefined    // undefined
0 || false           // false
"" || "default"      // "default"
```

**短路求值：**
```javascript
// && 左侧为假，不会执行右侧
false && console.log("不会执行");

// || 左侧为真，不会执行右侧
true || console.log("不会执行");
```

**实际应用：**
```javascript
// 默认值
function greet(name) {
  name = name || "Guest";
  console.log(`Hello, ${name}`);
}

// 条件执行
user && user.login();

// 链式访问
const city = user && user.address && user.address.city;
```

**ES2020 新增：空值合并（??）**
```javascript
// || 会将 0、"" 视为假值
0 || 10        // 10
"" || "text"   // "text"

// ?? 只将 null 和 undefined 视为空值
0 ?? 10        // 0
"" ?? "text"   // ""
null ?? 10     // 10
```

</details>

---

## 第 4 题 🟡

**类型：** 代码输出题  
**标签：** 位运算符

### 题目

以下代码的输出是什么？

```javascript
console.log(5 & 3);
console.log(5 | 3);
console.log(5 ^ 3);
console.log(~5);
```

**选项：**
- A. `1`, `7`, `6`, `-6`
- B. `2`, `6`, `4`, `-5`
- C. `3`, `7`, `2`, `-6`
- D. `1`, `5`, `6`, `-5`

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A

### 📖 解析

**位运算符按二进制位操作**

**1. 按位与（&）**
```javascript
5 & 3
// 5: 0101
// 3: 0011
// &: 0001 = 1
```

**2. 按位或（|）**
```javascript
5 | 3
// 5: 0101
// 3: 0011
// |: 0111 = 7
```

**3. 按位异或（^）**
```javascript
5 ^ 3
// 5: 0101
// 3: 0011
// ^: 0110 = 6
```

**4. 按位非（~）**
```javascript
~5
// 5:  00000000000000000000000000000101
// ~5: 11111111111111111111111111111010
// 补码表示 = -6
```

**按位非的计算公式：**
```javascript
~n = -(n + 1)

~5 = -(5 + 1) = -6
~0 = -1
~-1 = 0
```

**实际应用：**

**1. 检查奇偶数**
```javascript
function isEven(n) {
  return (n & 1) === 0;
}
isEven(4);  // true
isEven(5);  // false
```

**2. 交换变量**
```javascript
let a = 5, b = 3;
a = a ^ b;  // a = 5 ^ 3 = 6
b = a ^ b;  // b = 6 ^ 3 = 5
a = a ^ b;  // a = 6 ^ 5 = 3
console.log(a, b);  // 3, 5
```

**3. 检查 indexOf**
```javascript
// 传统方式
if (str.indexOf('hello') !== -1) {}

// 使用位运算
if (~str.indexOf('hello')) {}
// ~(-1) = 0（假值）
// ~(>=0) = 负数（真值）
```

**4. 取整（快速）**
```javascript
~~3.14    // 3
~~-3.14   // -3
3.14 | 0  // 3
```

</details>

---

## 第 5 题 🟡

**类型：** 代码输出题  
**标签：** 比较运算符

### 题目

以下代码的输出是什么？

```javascript
console.log('10' > 9);
console.log('10' > '9');
console.log('b' > 'a');
console.log('10' == 10);
console.log('10' === 10);
```

**选项：**
- A. `true`, `true`, `true`, `true`, `false`
- B. `true`, `false`, `true`, `true`, `false`
- C. `false`, `false`, `true`, `true`, `false`
- D. `true`, `true`, `true`, `false`, `false`

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**比较运算符的转换规则**

**1. `'10' > 9` → `true`**
```javascript
// 字符串与数字比较，字符串转数字
'10' > 9
Number('10') > 9
10 > 9  // true
```

**2. `'10' > '9'` → `false`**
```javascript
// 两个字符串比较，按字典序（Unicode）
'10' > '9'
// 比较第一个字符：'1'(49) vs '9'(57)
// 49 < 57
// false
```

**3. `'b' > 'a'` → `true`**
```javascript
// 字符串按 Unicode 码点比较
'b'.charCodeAt(0)  // 98
'a'.charCodeAt(0)  // 97
// 98 > 97 → true
```

**4. `'10' == 10` → `true`**
```javascript
// == 允许类型转换
'10' == 10
Number('10') == 10
10 == 10  // true
```

**5. `'10' === 10` → `false`**
```javascript
// === 严格相等，类型不同直接返回 false
'10' === 10  // false
```

---

**字符串比较规则：**

**按字典序（Unicode）逐字符比较**
```javascript
'apple' < 'banana'   // true
'10' < '9'          // true（'1' < '9'）
'10' < '2'          // true（'1' < '2'）
'ab' < 'abc'        // true（长度更短）
'Z' < 'a'           // true（65 < 97，大写在前）
```

**数字字符串的陷阱：**
```javascript
// 作为数字比较
'10' > 9            // true（转数字）
10 > '9'            // true（转数字）

// 作为字符串比较
'10' > '9'          // false（字典序）
'100' > '2'         // false（字典序）

// 排序时的问题
['1', '10', '2'].sort()  // ['1', '10', '2']（字典序）
[1, 10, 2].sort()        // [1, 10, 2]（默认转字符串）

// 正确的数字排序
[1, 10, 2].sort((a, b) => a - b)  // [1, 2, 10]
```

**最佳实践：**
```javascript
// ✅ 数字比较明确转换
Number('10') > Number('9')

// ✅ 使用严格相等
'10' === '10'

// ✅ 字符串比较使用 localeCompare
'a'.localeCompare('b')  // -1（a < b）
```

</details>

---

## 第 6 题 🟡

**类型：** 多选题  
**标签：** 赋值运算符

### 题目

以下哪些是 JavaScript 的赋值运算符？

**选项：**
- A. `+=`, `-=`, `*=`, `/=`
- B. `&&=`, `||=`, `??=`
- C. `%=`, `**=`
- D. `&=`, `|=`, `^=`

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A, B, C, D

### 📖 解析

**JavaScript 的赋值运算符（全部正确）**

**1. 算术赋值运算符（A, C）**
```javascript
let x = 10;

x += 5;   // x = x + 5 → 15
x -= 3;   // x = x - 3 → 12
x *= 2;   // x = x * 2 → 24
x /= 4;   // x = x / 4 → 6
x %= 4;   // x = x % 4 → 2
x **= 3;  // x = x ** 3 → 8（ES2016）
```

**2. 逻辑赋值运算符（B）- ES2021**
```javascript
// 逻辑与赋值（&&=）
let a = 10;
a &&= 20;  // a = a && 20 → 20

let b = 0;
b &&= 20;  // b = b && 20 → 0（短路）

// 逻辑或赋值（||=）
let c = null;
c ||= 'default';  // c = c || 'default' → 'default'

let d = 'value';
d ||= 'default';  // d = d || 'default' → 'value'（短路）

// 空值合并赋值（??=）
let e = null;
e ??= 10;  // e = e ?? 10 → 10

let f = 0;
f ??= 10;  // f = f ?? 10 → 0（0 不是 null/undefined）
```

**3. 位赋值运算符（D）**
```javascript
let x = 5;  // 0101

x &= 3;   // x = x & 3 → 1（0001）
x |= 6;   // x = x | 6 → 7（0111）
x ^= 2;   // x = x ^ 2 → 5（0101）
x <<= 1;  // x = x << 1 → 10（左移）
x >>= 1;  // x = x >> 1 → 5（右移）
```

---

**逻辑赋值运算符的实际应用**

**1. 设置默认值**
```javascript
// 使用 ||=
function setDefaults(config) {
  config.timeout ||= 3000;
  config.retries ||= 3;
  config.debug ||= false;
}

// 使用 ??=（更精确）
function setDefaults2(config) {
  config.timeout ??= 3000;
  config.retries ??= 3;
  config.debug ??= false;
}

const config = { timeout: 0 };
setDefaults(config);   // timeout 变成 3000（❌ 0 被误认为空）
setDefaults2(config);  // timeout 保持 0（✅ 0 是有效值）
```

**2. 懒初始化**
```javascript
let cache;

function getData() {
  // 只在首次调用时初始化
  cache ??= expensiveOperation();
  return cache;
}
```

**3. 条件更新**
```javascript
let user = { isAdmin: true, permissions: ['read'] };

// 只有 isAdmin 为 true 才添加权限
user.isAdmin &&= user.permissions.push('write');
```

**对比：**
```javascript
// ||= vs ??=
let a = 0;
a ||= 10;   // 10（0 是假值）
a ??= 10;   // 0（0 不是 null/undefined）

let b = '';
b ||= 'text';   // 'text'（'' 是假值）
b ??= 'text';   // ''（'' 不是 null/undefined）
```

</details>

---

## 第 7 题 🟡

**类型：** 代码输出题  
**标签：** 三元运算符

### 题目

以下代码的输出是什么？

```javascript
let age = 20;
let status = age >= 18 ? 'adult' : age >= 13 ? 'teen' : 'child';
console.log(status);
```

**选项：**
- A. `"adult"`
- B. `"teen"`
- C. `"child"`
- D. 报错

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A

### 📖 解析

**三元运算符的嵌套**

**执行过程：**
```javascript
age = 20;

// 从左到右求值
age >= 18 ? 'adult' : (age >= 13 ? 'teen' : 'child')
20 >= 18 ? 'adult' : ...
true ? 'adult' : ...
'adult'
```

**等价的 if-else：**
```javascript
let status;
if (age >= 18) {
  status = 'adult';
} else if (age >= 13) {
  status = 'teen';
} else {
  status = 'child';
}
```

**结合性：右结合**
```javascript
// 三元运算符是右结合的
a ? b : c ? d : e
// 等价于
a ? b : (c ? d : e)
// 而不是
(a ? b : c) ? d : e
```

---

**更多示例**

**1. 简单三元**
```javascript
let result = score >= 60 ? 'pass' : 'fail';
```

**2. 嵌套三元**
```javascript
let grade = 
  score >= 90 ? 'A' :
  score >= 80 ? 'B' :
  score >= 70 ? 'C' :
  score >= 60 ? 'D' : 'F';
```

**3. 返回函数**
```javascript
const func = condition ? () => 'yes' : () => 'no';
```

**4. 链式调用**
```javascript
const value = obj?.prop ? obj.prop : 'default';
```

**最佳实践：**
```javascript
// ✅ 简单条件
const msg = isOnline ? 'Online' : 'Offline';

// ✅ 单层嵌套（可读）
const status = age >= 18 ? 'adult' : age >= 13 ? 'teen' : 'child';

// ❌ 多层嵌套（难读）
const result = a ? b ? c ? d : e : f : g ? h : i;

// ✅ 改用 if-else
let result;
if (a) {
  if (b) {
    result = c ? d : e;
  } else {
    result = f;
  }
} else {
  result = g ? h : i;
}
```

</details>

---

## 第 8 题 🔴

**类型：** 代码分析题  
**标签：** 运算符优先级

### 题目

以下代码的输出是什么？

```javascript
let a = 1, b = 2, c = 3;
let result = a + b * c > 5 && b < c || c > 5;
console.log(result);
```

**选项：**
- A. `true`
- B. `false`
- C. `undefined`
- D. 报错

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A

### 📖 解析

**复杂表达式的求值顺序**

**步骤 1：按优先级分解**
```javascript
let result = a + b * c > 5 && b < c || c > 5;

// 优先级顺序：
// 1. * (乘法)
// 2. + (加法)
// 3. > < (比较)
// 4. && (逻辑与)
// 5. || (逻辑或)

// 添加括号明确优先级
let result = (((a + (b * c)) > 5) && (b < c)) || (c > 5);
```

**步骤 2：逐步求值**
```javascript
a = 1, b = 2, c = 3

// 第 1 步：b * c
2 * 3 = 6

// 第 2 步：a + 6
1 + 6 = 7

// 第 3 步：7 > 5
true

// 第 4 步：b < c
2 < 3 = true

// 第 5 步：true && true
true

// 第 6 步：c > 5
3 > 5 = false

// 第 7 步：true || false
true
```

**完整求值过程：**
```javascript
a + b * c > 5 && b < c || c > 5
1 + 2 * 3 > 5 && 2 < 3 || 3 > 5
1 + 6 > 5 && 2 < 3 || 3 > 5
7 > 5 && 2 < 3 || 3 > 5
true && true || false
true || false
true
```

---

**关键知识点**

**1. 运算符优先级表**
```
级别 1（最高）: ()
级别 2: * / %
级别 3: + -
级别 4: < > <= >=
级别 5: == != === !==
级别 6: &&
级别 7: ||
级别 8: ? :
级别 9（最低）: = += -=
```

**2. 短路求值**
```javascript
// && 遇到假值停止
false && console.log('不执行');  // false

// || 遇到真值停止
true || console.log('不执行');   // true

// 实际应用
let result = expr1() || expr2() || expr3();
// 返回第一个真值，后续不执行
```

**3. 复杂表达式的拆分**
```javascript
// ❌ 难以理解
let x = a + b * c > d && e < f || g > h;

// ✅ 拆分清晰
let sum = a + b * c;
let cond1 = sum > d && e < f;
let cond2 = g > h;
let x = cond1 || cond2;

// ✅ 或使用括号
let x = ((a + b * c) > d && e < f) || (g > h);
```

**4. 实际案例**
```javascript
// 复杂的条件判断
function canAccess(user, resource) {
  return (
    user.isAdmin ||
    (user.isOwner && resource.ownerId === user.id) ||
    (user.hasPermission('read') && resource.isPublic)
  );
}
```

**最佳实践：**
- ✅ 使用括号明确优先级
- ✅ 拆分复杂表达式
- ✅ 添加注释说明逻辑
- ❌ 避免过度依赖优先级记忆

</details>

---

## 第 9 题 🔴

**类型：** 代码输出题  
**标签：** 类型转换

### 题目

以下代码的输出是什么？

```javascript
console.log([] + []);
console.log([] + {});
console.log({} + []);
console.log({} + {});
```

**选项：**
- A. `""`, `"[object Object]"`, `0`, `NaN`
- B. `[]`, `[object Object]`, `[object Object]`, `[object Object][object Object]`
- C. `""`, `"[object Object]"`, `"[object Object]"`, `"[object Object][object Object]"`
- D. 报错

<details>
<summary>查看答案</summary>

### ✅ 正确答案：A（在大多数环境中）

### 📖 解析

**对象和数组的加法运算**

**⚠️ 注意：** 第 3 题在不同环境中可能有不同结果！

---

**1. `[] + []` → `""`**
```javascript
[] + []
// 两个数组转字符串
[].toString()  // ""
[].toString()  // ""
"" + ""        // ""
```

**2. `[] + {}` → `"[object Object]"`**
```javascript
[] + {}
// 数组转字符串
[].toString()  // ""
// 对象转字符串
({}).toString()  // "[object Object]"
"" + "[object Object]"  // "[object Object]"
```

**3. `{} + []` → `0`（或 `"[object Object]"`）**
```javascript
// 在语句位置（Chrome 控制台）
{} + []
// {} 被解析为代码块（不是对象）
// 实际执行：+[]
// 等价于：Number([])
// [].toString() = ""
// Number("") = 0
// 结果：0

// 在表达式位置
({} + [])
// {} 被解析为对象
({}).toString() + [].toString()
"[object Object]" + ""
// 结果："[object Object]"
```

**4. `{} + {}` → `NaN`（或 `"[object Object][object Object]"`）**
```javascript
// 在语句位置
{} + {}
// 第一个 {} 是代码块
// 实际执行：+{}
// Number({}) = NaN
// 结果：NaN

// 在表达式位置
({} + {})
({}).toString() + ({}).toString()
"[object Object]" + "[object Object]"
// 结果："[object Object][object Object]"
```

---

**ToPrimitive 转换规则**

**数组转原始值：**
```javascript
[].valueOf()    // []（返回自身）
[].toString()   // ""（使用这个）

[1].toString()      // "1"
[1, 2].toString()   // "1,2"
```

**对象转原始值：**
```javascript
({}).valueOf()   // {}（返回自身）
({}).toString()  // "[object Object]"（使用这个）
```

**加法运算的规则：**
1. 将操作数转换为原始值（ToPrimitive）
2. 如果有一个是字符串，进行字符串拼接
3. 否则，转换为数字进行加法

---

**更多奇怪的例子**

```javascript
// 数组加法
[1] + [2]        // "12"
[1, 2] + [3, 4]  // "1,23,4"

// 对象加法
{a: 1} + {b: 2}  // NaN 或 "[object Object][object Object]"

// 混合运算
[] + null        // "null"
[] + undefined   // "undefined"
[] + true        // "true"
[] + 1           // "1"

// 一元加号
+[]              // 0
+{}              // NaN
+[1]             // 1
+[1, 2]          // NaN
```

**避免歧义：**
```javascript
// ✅ 明确意图
String([]) + String([])
Number({}) + Number({})

// ✅ 使用括号
({} + [])
({} + {})

// ✅ 避免对象/数组直接相加
const arr1 = [], arr2 = [];
arr1.concat(arr2)  // 合并数组

const obj1 = {}, obj2 = {};
Object.assign({}, obj1, obj2)  // 合并对象
```

</details>

---

## 第 10 题 🔴

**类型：** 综合题  
**标签：** 逗号运算符

### 题目

以下代码的输出是什么？

```javascript
let x = (1, 2, 3);
console.log(x);

let y = (console.log('a'), console.log('b'), 'result');
console.log(y);
```

**选项：**
- A. `1`, `a`, `b`, `"result"`
- B. `3`, `a`, `b`, `"result"`
- C. `[1, 2, 3]`, `a`, `b`, `"result"`
- D. `3`, `"result"`, `a`, `b`

<details>
<summary>查看答案</summary>

### ✅ 正确答案：B

### 📖 解析

**逗号运算符：返回最后一个表达式的值**

**执行过程：**

**代码 1：**
```javascript
let x = (1, 2, 3);
// 逗号运算符依次执行 1, 2, 3
// 返回最后一个表达式的值：3
console.log(x);  // 3
```

**代码 2：**
```javascript
let y = (console.log('a'), console.log('b'), 'result');
// 1. 执行 console.log('a')，输出 'a'，返回 undefined
// 2. 执行 console.log('b')，输出 'b'，返回 undefined
// 3. 返回 'result'
// y = 'result'

console.log(y);  // 'result'
```

**完整输出顺序：**
```
3
a
b
result
```

---

**逗号运算符的特性**

**1. 依次求值，返回最后一个**
```javascript
let a = (1 + 2, 3 + 4, 5 + 6);
console.log(a);  // 11

let b = (console.log('first'), console.log('second'), 42);
// 输出：first, second
// b = 42
```

**2. 优先级最低**
```javascript
// 需要括号
let x = (1, 2);  // x = 2
let y = 1, 2;    // SyntaxError

// 在函数调用中
func(1, 2)       // 两个参数
func((1, 2))     // 一个参数（值为 2）
```

**3. 在 for 循环中使用**
```javascript
for (let i = 0, j = 10; i < 5; i++, j--) {
  console.log(i, j);
}
// 0 10
// 1 9
// 2 8
// 3 7
// 4 6
```

---

**实际应用场景**

**1. 简化代码**
```javascript
// 多个操作合并
let x = (init(), process(), getResult());

// 等价于
init();
process();
let x = getResult();
```

**2. 条件赋值**
```javascript
let result = (validate(), transform(), save());
```

**3. 返回语句**
```javascript
function doWork() {
  return (cleanup(), finalValue);
}
```

**4. 箭头函数中**
```javascript
const handler = () => (log('called'), process());
```

---

**常见陷阱**

**1. 变量声明**
```javascript
// ❌ 错误：多个变量
let a = 1, b = 2, c = 3;
// 这不是逗号运算符，是变量声明语法

// 逗号运算符需要括号
let result = (a = 1, b = 2, c = 3);
console.log(result);  // 3
console.log(a, b, c);  // 1, 2, 3
```

**2. 函数参数**
```javascript
function test(a, b) {
  console.log(a, b);
}

test(1, 2);      // 1, 2（两个参数）
test((1, 2));    // 2, undefined（一个参数）
```

**3. 数组**
```javascript
let arr1 = [1, 2, 3];      // 数组字面量
let arr2 = [(1, 2, 3)];    // [3]（逗号运算符）
```

**最佳实践：**
- ✅ 在 for 循环中使用
- ✅ 简化简单的顺序操作
- ❌ 避免过度使用（降低可读性）
- ❌ 不要与数组/函数参数混淆

</details>

---

**本章总结：**
- ✅ 运算符优先级和结合性
- ✅ 递增递减运算符
- ✅ 逻辑运算符和短路求值
- ✅ 位运算符
- ✅ 比较运算符和类型转换
- ✅ 赋值运算符（包括 ES2021 新增）
- ✅ 三元运算符
- ✅ 逗号运算符

**下一章：** [第 3 章：流程控制与异常处理](./chapter-03.md)
